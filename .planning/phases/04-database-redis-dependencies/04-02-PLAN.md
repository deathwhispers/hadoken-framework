---
phase: 04-database-redis-dependencies
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hadoken-dependencies/pom.xml
  - hadoken-redis-spring-boot-starter/pom.xml
autonomous: true
requirements:
  - REDIS-01
  - REDIS-02
must_haves:
  truths:
    - "Redisson 版本已升级到支持 JDK 25"
    - "Spring Data Redis 版本通过 Spring Boot BOM 正确管理"
    - "Redis 相关模块在 JDK 25 下编译成功"
  artifacts:
    - path: "hadoken-dependencies/pom.xml"
      provides: "Redis 相关依赖版本配置"
      contains: "<redisson.version>"
    - path: "hadoken-redis-spring-boot-starter/pom.xml"
      provides: "Redis Starter 依赖"
      contains: "redisson-spring-boot-starter"
  key_links:
    - from: "hadoken-dependencies/pom.xml"
      to: "hadoken-redis-spring-boot-starter/pom.xml"
      via: "依赖版本继承"
      pattern: "<redisson.version>"
    - from: "Spring Boot 3.5.5 BOM"
      to: "Spring Data Redis"
      via: "BOM 版本管理"
      pattern: "spring-boot-dependencies"
---

<objective>
验证和升级 Redis 相关组件到 JDK 25 兼容版本。

Purpose: 确保缓存层（Redisson、Spring Data Redis）在 JDK 25 环境下正常工作。
Output: 更新后的 Redis 依赖版本配置，验证结果记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-database-redis-dependencies/04-RESEARCH.md

# 当前依赖版本配置
hadoken-dependencies/pom.xml 中相关配置：
```xml
<!-- Redis -->
<redisson.version>3.45.1</redisson.version>
```

# Spring Boot BOM 管理
Spring Data Redis 版本通过 Spring Boot 3.5.5 BOM 管理：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-dependencies</artifactId>
    <version>${spring-boot.version}</version>
    <type>pom</type>
    <scope>import</scope>
</dependency>
```

# 研究中的关键发现
- Redisson 3.45.1 需要验证 JDK 25 支持状态
- Spring Data Redis 版本通过 Spring Boot BOM 管理，需要验证是否兼容 JDK 25
- Redisson 的序列化机制可能需要调整以兼容 JDK 25

# 需求覆盖
- REDIS-01: Redisson 升级至 JDK 25 兼容版本
- REDIS-02: Spring Data Redis 版本验证（随 Spring Boot）
</context>

<tasks>

<task type="auto">
  <name>任务1: 验证 Redis 依赖的 JDK 25 兼容性</name>
  <files>hadoken-dependencies/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - .planning/phases/04-database-redis-dependencies/04-RESEARCH.md
    - hadoken-redis-spring-boot-starter/pom.xml
  </read_first>
  <action>
    1. 检查 Redisson 3.45.1 的官方发布说明，确认是否支持 JDK 25
       - 查看 Redisson GitHub releases: https://github.com/redisson/redisson/releases
       - 检查官方文档中的 JDK 兼容性说明
       - 搜索 issue 跟踪器中关于 JDK 25 的问题

    2. 验证 Spring Data Redis 的兼容性：
       - Spring Data Redis 版本通过 Spring Boot 3.5.5 BOM 管理
       - 检查 Spring Boot 3.5.5 官方文档，确认 Spring Data Redis 版本
       - 验证该版本是否支持 JDK 25

    3. 检查 Redisson 的序列化兼容性：
       - JDK 25 可能修改了序列化机制
       - 确认 Redisson 的默认序列化器（Jackson、Kryo、FST）是否兼容 JDK 25
       - 检查是否有已知的序列化问题

    4. 记录兼容性状态到 .planning/phases/04-database-redis-dependencies/redis-compatibility.md

    验证方法：
    - 搜索官方文档和 GitHub issues
    - 检查 Maven Central 的编译要求
    - 查看社区讨论和升级指南
  </action>
  <verify>
    <automated>test -f .planning/phases/04-database-redis-dependencies/redis-compatibility.md && grep -q "Redisson" .planning/phases/04-database-redis-dependencies/redis-compatibility.md && grep -q "Spring Data Redis" .planning/phases/04-database-redis-dependencies/redis-compatibility.md</automated>
  </verify>
  <done>Redis 兼容性报告文件创建完成，包含 Redisson 和 Spring Data Redis 的 JDK 25 兼容性状态</done>
</task>

<task type="auto">
  <name>任务2: 更新 Redis 依赖版本到 JDK 25 兼容版本</name>
  <files>hadoken-dependencies/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - .planning/phases/04-database-redis-dependencies/redis-compatibility.md
    - hadoken-redis-spring-boot-starter/pom.xml
  </read_first>
  <action>
    根据兼容性报告的结果，更新 hadoken-dependencies/pom.xml 中的 Redis 相关依赖：

    1. 如果 Redisson 3.45.1 不支持 JDK 25：
       - 查找支持 JDK 25 的最新稳定版本（如 3.46.0+）
       - 更新 redisson.version 属性
       - 示例：`<redisson.version>3.46.0</redisson.version>`

    2. 如果 Spring Data Redis 需要单独升级（不通过 BOM）：
       - 添加 spring-data-redis.version 属性
       - 在 dependencyManagement 中添加显式版本管理
       - 注意：通常通过 Spring Boot BOM 管理，不需要单独升级

    3. 如果 Redisson 需要序列化配置调整：
       - 记录配置建议到 redis-compatibility.md
       - 后续在功能验证阶段实施

    4. 如果所有组件都兼容 JDK 25，则保持当前版本不变，但在报告中明确记录。

    重要：Redisson 升级可能涉及 API 变更，需要检查是否有破坏性变更。
  </action>
  <verify>
    <automated>grep -q "<redisson.version>" hadoken-dependencies/pom.xml && echo "Redisson 版本配置存在"</automated>
  </verify>
  <done>hadoken-dependencies/pom.xml 中的 Redis 依赖版本已更新（或确认无需更新），支持 JDK 25</done>
</task>

<task type="auto">
  <name>任务3: 验证 Redis 模块编译和依赖解析</name>
  <files>hadoken-redis-spring-boot-starter/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - hadoken-redis-spring-boot-starter/pom.xml
    - .planning/phases/04-database-redis-dependencies/redis-compatibility.md
  </read_first>
  <action>
    1. 清理并重新编译 hadoken-redis-spring-boot-starter 模块：
       ```bash
       cd hadoken-redis-spring-boot-starter
       mvn clean compile -DskipTests
       ```

    2. 验证依赖解析正确：
       ```bash
       mvn dependency:tree -Dincludes=org.redisson:redisson-spring-boot-starter
       mvn dependency:tree -Dincludes=org.springframework.boot:spring-boot-starter-data-redis
       mvn dependency:tree -Dincludes=org.springframework.boot:spring-boot-starter-cache
       ```

    3. 检查编译错误或警告：
       - 特别关注与 JDK 25 相关的字节码警告
       - 检查 Redisson 相关类的导入和初始化
       - 验证序列化相关的类是否可访问

    4. 如果编译失败：
       - 分析错误信息，判断是否是版本不兼容
       - 根据错误调整版本或配置
       - 如果是因为 Redisson API 变更，可能需要更新代码

    5. 记录编译结果到 redis-compatibility.md

    注意：本阶段只关注编译兼容性，不涉及运行时功能测试。
  </action>
  <verify>
    <automated>cd hadoken-redis-spring-boot-starter && mvn clean compile -DskipTests 2>&1 | tail -20 | grep -q "BUILD SUCCESS"</automated>
  </verify>
  <done>hadoken-redis-spring-boot-starter 模块在 JDK 25 下编译成功，所有 Redis 依赖正确解析</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| Redis 客户端 → Redis 服务器 | 网络通信可能被拦截或篡改 |
| 序列化数据 → 存储 | Redis 中存储的序列化数据可能被恶意反序列化 |

## STRIDE 威胁登记

| 威胁ID | 类别 | 组件 | 处置 | 缓解计划 |
|--------|------|------|------|----------|
| T-04-05 | 信息泄露 | Redis 数据传输 | 缓解 | 使用 Redis SSL/TLS 加密传输（如果支持） |
| T-04-06 | 篡改 | Redis 序列化数据 | 缓解 | 使用安全的序列化器（如 Jackson），避免 Java 原生序列化 |
| T-04-07 | 拒绝服务 | Redis 连接池 | 缓解 | 配置合理的连接池大小和超时时间 |
| T-04-08 | 权限提升 | Redisson 分布式锁 | 接受 | 使用公平锁和合理的锁超时时间，避免死锁 |
</threat_model>

<verification>
1. 运行整体项目编译检查：
   ```bash
   mvn clean compile -DskipTests -pl hadoken-redis-spring-boot-starter
   ```
2. 验证 Redisson 版本：
   ```bash
   mvn help:effective-pom -pl hadoken-redis-spring-boot-starter | grep -A2 -B2 "redisson"
   ```
3. 检查 Spring Data Redis 版本：
   ```bash
   mvn dependency:tree -pl hadoken-redis-spring-boot-starter -Dincludes=org.springframework.data:spring-data-redis | grep "spring-data-redis"
   ```
4. 验证兼容性报告：
   ```bash
   cat .planning/phases/04-database-redis-dependencies/redis-compatibility.md | grep -E "(兼容|不兼容|建议|序列化)" | head -10
   ```
</verification>

<success_criteria>
1. hadoken-dependencies/pom.xml 中的 Redis 相关依赖版本已更新或确认为 JDK 25 兼容
2. hadoken-redis-spring-boot-starter 模块在 JDK 25 下编译成功
3. Redisson 和 Spring Data Redis 的 JDK 25 兼容性已验证
4. Redis 兼容性报告文件已创建，记录验证结果和建议
</success_criteria>

<output>
完成后，创建 .planning/phases/04-database-redis-dependencies/04-02-SUMMARY.md
</output>