---
phase: 04-database-redis-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hadoken-dependencies/pom.xml
  - hadoken-mybatis-spring-boot-starter/pom.xml
  - hadoken-redis-spring-boot-starter/pom.xml
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
must_haves:
  truths:
    - "MyBatis-Plus 版本已验证或升级，支持 JDK 25"
    - "Druid 连接池版本已验证，与 Spring Boot 3.5.5 兼容"
    - "MySQL Connector 版本已验证，支持 JDK 25"
    - "Dynamic Datasource 版本已验证，在 JDK 25 下正常工作"
  artifacts:
    - path: "hadoken-dependencies/pom.xml"
      provides: "数据库相关依赖版本配置"
      contains: "<mybatis-plus.version>", "<druid.version>", "<mysql.version>", "<dynamic-datasource.version>"
    - path: "hadoken-mybatis-spring-boot-starter/pom.xml"
      provides: "MyBatis-Plus Starter 依赖"
      contains: "mybatis-plus-spring-boot3-starter"
  key_links:
    - from: "hadoken-dependencies/pom.xml"
      to: "hadoken-mybatis-spring-boot-starter/pom.xml"
      via: "依赖版本继承"
      pattern: "<mybatis-plus.version>"
---

<objective>
验证和升级 MyBatis-Plus 及相关数据库组件到 JDK 25 兼容版本。

Purpose: 确保数据库访问层（ORM、连接池、驱动、多数据源）在 JDK 25 环境下正常工作。
Output: 更新后的依赖版本配置，验证结果记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-database-redis-dependencies/04-RESEARCH.md

# 当前依赖版本配置
hadoken-dependencies/pom.xml 中相关配置：
```xml
<!-- 数据库相关依赖 -->
<druid.version>1.2.24</druid.version>
<mybatis.version>3.5.19</mybatis.version>
<mybatis-plus.version>3.5.12</mybatis-plus.version>
<dynamic-datasource.version>4.3.1</dynamic-datasource.version>
<mysql.version>8.4.0</mysql.version>
<!-- 国产数据库 -->
<dm8.jdbc.version>8.1.3.140</dm8.jdbc.version>
<kingbase.jdbc.version>8.6.0</kingbase.jdbc.version>
<opengauss.jdbc.version>5.1.0</opengauss.jdbc.version>
<taos.version>3.3.3</taos.version>
```

# 研究中的关键发现
- MyBatis-Plus 3.5.12 需要验证 JDK 25 兼容性
- Druid 1.2.24 需要验证与 Spring Boot 3.5.5 的集成
- MySQL Connector/J 8.4.0 需要验证 JDK 25 支持
- Dynamic Datasource 4.3.1 需要验证在 JDK 25 下正常工作

# 需求覆盖
- DB-01: MyBatis-Plus 版本验证/升级至 JDK 25 兼容版本
- DB-02: Druid 连接池版本验证/升级
- DB-03: MySQL Connector 版本验证/升级
- DB-04: Dynamic Datasource 版本验证/升级
</context>

<tasks>

<task type="auto">
  <name>任务1: 验证当前数据库依赖的JDK 25兼容性</name>
  <files>hadoken-dependencies/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - .planning/phases/04-database-redis-dependencies/04-RESEARCH.md
  </read_first>
  <action>
    1. 检查 MyBatis-Plus 3.5.12 的官方发布说明，确认是否支持 JDK 25
    2. 检查 Druid 1.2.24 的官方文档，确认与 Spring Boot 3.5.5 的兼容性
    3. 检查 MySQL Connector/J 8.4.0 的官方文档，确认 JDK 25 支持
    4. 检查 Dynamic Datasource 4.3.1 的官方文档，确认 JDK 25 兼容性
    5. 检查国产数据库驱动（达梦、金仓、OpenGauss、TDengine）的 JDK 25 兼容性
    6. 记录每个组件的兼容性状态到临时文件 .planning/phases/04-database-redis-dependencies/compatibility-report.md

    验证方法：
    - 使用 mvn dependency:tree 检查依赖树
    - 搜索官方 GitHub releases 和 issue 跟踪器
    - 检查 Maven Central 的编译要求
    
    如果发现不兼容，需要：
    - 查找支持 JDK 25 的最新稳定版本
    - 记录版本升级建议
  </action>
  <verify>
    <automated>test -f .planning/phases/04-database-redis-dependencies/compatibility-report.md && grep -q "MyBatis-Plus" .planning/phases/04-database-redis-dependencies/compatibility-report.md && grep -q "Druid" .planning/phases/04-database-redis-dependencies/compatibility-report.md && grep -q "MySQL Connector" .planning/phases/04-database-redis-dependencies/compatibility-report.md</automated>
  </verify>
  <done>兼容性报告文件创建完成，包含所有数据库组件的 JDK 25 兼容性状态</done>
</task>

<task type="auto">
  <name>任务2: 更新数据库依赖版本到 JDK 25 兼容版本</name>
  <files>hadoken-dependencies/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - .planning/phases/04-database-redis-dependencies/compatibility-report.md
    - hadoken-mybatis-spring-boot-starter/pom.xml
  </read_first>
  <action>
    根据兼容性报告的结果，更新 hadoken-dependencies/pom.xml 中的数据库相关依赖版本：

    1. 如果 MyBatis-Plus 3.5.12 不支持 JDK 25，升级到支持的最新稳定版本（如 3.5.13+）
    2. 如果 Druid 1.2.24 与 Spring Boot 3.5.5 不兼容，升级到兼容版本（如 1.2.25+）
    3. 如果 MySQL Connector/J 8.4.0 不支持 JDK 25，升级到支持版本（如 8.4.1+）
    4. 如果 Dynamic Datasource 4.3.1 不支持 JDK 25，升级到支持版本（如 4.3.2+）
    5. 如果国产数据库驱动需要升级，更新相应版本号
    6. 确保所有版本变更在 properties 标签内更新

    更新示例：
    ```xml
    <!-- 数据库相关依赖 -->
    <mybatis-plus.version>3.5.13</mybatis-plus.version> <!-- 如果需要升级 -->
    <druid.version>1.2.25</druid.version> <!-- 如果需要升级 -->
    <mysql.version>8.4.1</mysql.version> <!-- 如果需要升级 -->
    <dynamic-datasource.version>4.3.2</dynamic-datasource.version> <!-- 如果需要升级 -->
    ```

    如果所有组件都兼容 JDK 25，则保持当前版本不变，但需要在兼容性报告中明确记录。
  </action>
  <verify>
    <automated>mvn dependency:tree -Dincludes=com.baomidou:mybatis-plus-spring-boot3-starter -q 2>/dev/null | grep -q "mybatis-plus" && echo "MyBatis-Plus 依赖检查通过"</automated>
  </verify>
  <done>hadoken-dependencies/pom.xml 中的数据库依赖版本已更新（或确认无需更新），所有版本支持 JDK 25</done>
</task>

<task type="auto">
  <name>任务3: 验证数据库模块编译和依赖解析</name>
  <files>hadoken-mybatis-spring-boot-starter/pom.xml</files>
  <read_first>
    - hadoken-dependencies/pom.xml
    - hadoken-mybatis-spring-boot-starter/pom.xml
    - .planning/phases/04-database-redis-dependencies/compatibility-report.md
  </read_first>
  <action>
    1. 清理并重新编译 hadoken-mybatis-spring-boot-starter 模块：
       ```bash
       cd hadoken-mybatis-spring-boot-starter
       mvn clean compile -DskipTests
       ```
    2. 验证依赖解析正确：
       ```bash
       mvn dependency:tree -Dincludes=com.alibaba:druid-spring-boot-3-starter
       mvn dependency:tree -Dincludes=com.baomidou:mybatis-plus-spring-boot3-starter
       mvn dependency:tree -Dincludes=com.baomidou:dynamic-datasource-spring-boot3-starter
       mvn dependency:tree -Dincludes=com.mysql:mysql-connector-j
       ```
    3. 检查是否有编译错误或警告，特别是与 JDK 25 相关的字节码警告
    4. 如果编译失败，根据错误信息调整版本或配置
    5. 记录编译结果到兼容性报告

    注意：如果使用测试容器进行集成测试，可能需要跳过，因为本阶段只关注编译兼容性。
  </action>
  <verify>
    <automated>cd hadoken-mybatis-spring-boot-starter && mvn clean compile -DskipTests 2>&1 | tail -20 | grep -q "BUILD SUCCESS"</automated>
  </verify>
  <done>hadoken-mybatis-spring-boot-starter 模块在 JDK 25 下编译成功，所有数据库依赖正确解析</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 外部依赖源 → 构建系统 | Maven Central 仓库下载的依赖包可能被篡改 |
| 数据库驱动 → 应用服务器 | 数据库驱动可能包含恶意代码 |

## STRIDE 威胁登记

| 威胁ID | 类别 | 组件 | 处置 | 缓解计划 |
|--------|------|------|------|----------|
| T-04-01 | 篡改 | Maven 依赖 | 接受 | 使用官方 Maven Central 仓库，验证依赖签名（如果可用） |
| T-04-02 | 信息泄露 | 数据库连接配置 | 缓解 | 确保配置文件不包含硬编码的数据库密码，使用环境变量 |
| T-04-03 | 拒绝服务 | 数据库连接池 | 缓解 | 配置合理的连接池大小和超时时间，防止资源耗尽 |
| T-04-04 | 权限提升 | 数据库驱动 | 接受 | 使用官方发布的驱动版本，避免使用未知来源的驱动 |
</threat_model>

<verification>
1. 运行整体项目编译检查：
   ```bash
   mvn clean compile -DskipTests -pl hadoken-mybatis-spring-boot-starter
   ```
2. 验证依赖版本一致性：
   ```bash
   mvn help:effective-pom -pl hadoken-mybatis-spring-boot-starter | grep -A2 -B2 "mybatis-plus\|druid\|mysql\|dynamic-datasource"
   ```
3. 检查兼容性报告是否完整：
   ```bash
   cat .planning/phases/04-database-redis-dependencies/compatibility-report.md | grep -E "(兼容|不兼容|建议)" | head -10
   ```
</verification>

<success_criteria>
1. hadoken-dependencies/pom.xml 中的数据库相关依赖版本已更新或确认为 JDK 25 兼容
2. hadoken-mybatis-spring-boot-starter 模块在 JDK 25 下编译成功
3. 所有数据库组件（MyBatis-Plus、Druid、MySQL Connector、Dynamic Datasource）的 JDK 25 兼容性已验证
4. 兼容性报告文件已创建，记录每个组件的验证结果和建议
</success_criteria>

<output>
完成后，创建 .planning/phases/04-database-redis-dependencies/04-01-SUMMARY.md
</output>