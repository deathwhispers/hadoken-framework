---
phase: 02-security-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java
  - hadoken-security-spring-boot-starter/src/test/java/com/github/hadoken/framework/security/autoconfigure/SecurityPropertiesTest.java
  - hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java
  - hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java
  - hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultDecryptor.java
autonomous: true
requirements:
  - SEC-04
user_setup: []

must_haves:
  truths:
    - "SecurityProperties中的默认密码'123456'已移除"
    - "DBAESUtil中的硬编码密钥'rhy'和固定IV已移除"
    - "数据库字段加密使用配置注入的密钥"
    - "DefaultEncryptor/DefaultDecryptor中的e.printStackTrace()已替换为log.error()"
  artifacts:
    - path: "hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java"
      provides: "更新后的安全配置属性"
      contains: "private String mockSecret" (无默认值)
    - path: "hadoken-security-spring-boot-starter/src/test/java/com/github/hadoken/framework/security/autoconfigure/SecurityPropertiesTest.java"
      provides: "安全属性测试覆盖"
      contains: "@Test void testMockSecretValidation()"
    - path: "hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java"
      provides: "更新后的数据库AES工具"
      contains: "通过配置注入密钥和IV"
  key_links:
    - from: "SecurityProperties.java"
      to: "application.yml配置"
      via: "必需配置验证"
      pattern: "@NotEmpty(message = \"mock模式的秘钥不能为空\")"
    - from: "DBAESUtil.java"
      to: "EncryptProperties.java"
      via: "数据库加密配置注入"
      pattern: "@Autowired private EncryptProperties encryptProperties"
    - from: "DefaultEncryptor.java"
      to: "slf4j日志"
      via: "异常日志记录"
      pattern: "log.error(\"加密失败\", ex)"
---

<objective>
移除所有硬编码默认密码和不安全配置，包括SecurityProperties的默认密码'123456'、DBAESUtil的硬编码密钥'rhy'和固定IV，修复异常处理中的e.printStackTrace()。

Purpose: 消除默认密码安全风险，实现所有敏感配置外部化，改进异常处理符合日志最佳实践。
Output: 更新的SecurityProperties、DBAESUtil、DefaultEncryptor/Decryptor，以及相应的单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/02-security-fixes/02-CONTEXT.md
@.planning/phases/02-security-fixes/02-RESEARCH.md
@.planning/phases/02-security-fixes/02-VALIDATION.md

# 当前默认密码问题
hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java

# 数据库加密硬编码问题
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java

# 异常处理问题
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultDecryptor.java

# 加密配置（依赖）
hadoken-common/src/main/java/com/github/hadoken/common/config/EncryptProperties.java

<interfaces>
<!-- 当前SecurityProperties默认密码问题 -->
从 SecurityProperties.java 提取：

```java
public class SecurityProperties {
    @NotEmpty(message = "mock模式的秘钥不能为空") // 这里设置了一个默认值，因为实际上只有 mockEnable 为 true 时才需要配置。
    private String mockSecret = "123456"; // 需要移除默认值
}
```

<!-- 当前DBAESUtil硬编码问题 -->
从 DBAESUtil.java 提取：

```java
public class DBAESUtil {
    private static final String DEFAULT_V = "6859505890402435"; // 固定IV，需要配置化
    private static final String KEY = "rhy"; // 硬编码密钥，需要配置化
}
```

<!-- 当前异常处理问题 -->
从 DefaultEncryptor.java 提取：

```java
public class DefaultEncryptor implements Encryptor {
    public String encrypt(String plaintext) {
        try {
            return DBAESUtil.encrypt(plaintext);
        } catch (Exception e) {
            e.printStackTrace(); // 需要替换为log.error()
            return plaintext;
        }
    }
}
```

<!-- 加密配置（来自Plan 01） -->
从 EncryptProperties.java 提取：

```java
public class EncryptProperties {
    @NotEmpty(message = "AES-256密钥不能为空")
    private String aesKey; // 可用于数据库加密
    
    // 可以扩展数据库加密专用配置
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新SecurityProperties移除默认密码</name>
  <files>hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java</files>
  <read_first>
hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java
hadoken-common/src/main/java/com/github/hadoken/common/config/EncryptProperties.java
  </read_first>
  <behavior>
- 测试1: mockSecret字段移除默认值"123456"
- 测试2: 保持@NotEmpty验证注解
- 测试3: 注释更新，说明只有mockEnable为true时才需要配置
- 测试4: 配置缺失时验证失败
- 测试5: 保持其他字段不变
  </behavior>
  <action>
修改 hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java 文件：

```java
package com.github.hadoken.framework.security.autoconfigure;

import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.NotNull;
import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.validation.annotation.Validated;

import java.time.Duration;

@ConfigurationProperties(prefix = "hadoken.security")
@Validated
@Data
public class SecurityProperties {

    /**
     * HTTP 请求时，访问令牌的请求 Header
     */
    @NotEmpty(message = "Token Header 不能为空")
    private String tokenHeader;

    /**
     * Token 过期时间,分钟
     */
    @NotNull(message = "Token 过期时间不能为空")
    private Duration tokenTimeout;

    /**
     * Token 秘钥
     */
    @NotEmpty(message = "Token 秘钥不能为空")
    private String tokenSecret;

    /**
     * Session 过期时间
     * <p>
     * 当 User 用户超过当前时间未操作，则 Session 会过期
     */
    @NotNull(message = "Session 过期时间不能为空")
    private Duration sessionTimeout;

    /**
     * mock 模式的开关
     */
    @NotNull(message = "mock 模式的开关不能为空")
    private Boolean mockEnable;

    /**
     * mock 模式的秘钥
     * <p>
     * 注意：只有当 mockEnable 为 true 时才需要配置此属性。
     * 必须配置一个安全的秘钥，不能使用默认值。
     */
    @NotEmpty(message = "mock模式的秘钥不能为空")
    private String mockSecret;

    /**
     * 验证配置完整性
     *
     * @throws IllegalStateException 如果必需配置缺失
     */
    public void validate() {
        if (Boolean.TRUE.equals(mockEnable)) {
            if (mockSecret == null || mockSecret.trim().isEmpty()) {
                throw new IllegalStateException(
                    "当 hadoken.security.mock-enable 为 true 时，必须配置 hadoken.security.mock-secret"
                );
            }
            // 简单安全检查：避免使用常见弱密码
            if ("123456".equals(mockSecret) || "password".equals(mockSecret) || 
                "admin".equals(mockSecret) || "test".equals(mockSecret)) {
                throw new IllegalStateException(
                    "mock-secret 不能使用常见弱密码，请设置一个安全的秘钥"
                );
            }
        }
    }
}
```

关键修改：
1. 移除 `mockSecret` 字段的默认值 `= "123456"`
2. 更新注释，明确说明只有 `mockEnable` 为 true 时才需要配置
3. 添加 `validate()` 方法进行配置完整性验证
4. 在 `validate()` 方法中添加简单安全检查，避免常见弱密码
5. 保持其他字段和验证注解不变
6. 遵循项目配置属性模式
  </action>
  <verify>
    <automated>grep -n "private String mockSecret" hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java</automated>
  </verify>
  <done>SecurityProperties.java更新完成，默认密码"123456"已移除，添加配置验证</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建SecurityProperties测试文件</name>
  <files>hadoken-security-spring-boot-starter/src/test/java/com/github/hadoken/framework/security/autoconfigure/SecurityPropertiesTest.java</files>
  <read_first>
hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java
hadoken-common/src/test/java/com/github/hadoken/common/config/EncryptPropertiesTest.java
  </read_first>
  <behavior>
- 测试1: mockSecret字段无默认值
- 测试2: mockEnable为true时mockSecret必需配置
- 测试3: mockEnable为false时mockSecret可为空
- 测试4: 常见弱密码验证失败
- 测试5: 其他字段验证正常
- 测试6: validate()方法正确工作
  </behavior>
  <action>
在 hadoken-security-spring-boot-starter/src/test/java/com/github/hadoken/framework/security/autoconfigure/ 目录下创建 SecurityPropertiesTest.java 文件：

```java
package com.github.hadoken.framework.security.autoconfigure;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.TestPropertySource;

import java.time.Duration;

import static org.junit.jupiter.api.Assertions.*;

/**
 * SecurityProperties 单元测试
 *
 * @author yanggj
 * @version 1.0.0
 * @date 2026/05/29
 */
@SpringBootTest
@TestPropertySource(properties = {
    "hadoken.security.token-header=Authorization",
    "hadoken.security.token-timeout=30m",
    "hadoken.security.token-secret=secure-token-secret-123",
    "hadoken.security.session-timeout=60m",
    "hadoken.security.mock-enable=false"
    // mock-secret 不配置，因为 mock-enable=false
})
class SecurityPropertiesTest {

    @Test
    void testPropertiesBinding() {
        // 测试将通过Spring Boot的自动配置执行
        // 如果属性绑定成功，测试会通过
        assertTrue(true, "安全配置属性绑定测试");
    }

    @Test
    void testMockSecretNoDefaultValue() {
        SecurityProperties properties = new SecurityProperties();
        
        // 验证mockSecret没有默认值
        assertNull(properties.getMockSecret(), "mockSecret应没有默认值");
        
        // 设置值后可以获取
        properties.setMockSecret("custom-secret");
        assertEquals("custom-secret", properties.getMockSecret());
    }

    @Test
    void testValidateMockEnabledWithSecret() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(true);
        properties.setMockSecret("secure-mock-secret-123");
        
        // 验证不应抛出异常
        assertDoesNotThrow(properties::validate);
    }

    @Test
    void testValidateMockEnabledWithoutSecret() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(true);
        // mockSecret 未设置
        
        IllegalStateException exception = assertThrows(IllegalStateException.class, properties::validate);
        assertTrue(exception.getMessage().contains("hadoken.security.mock-secret"));
    }

    @Test
    void testValidateMockDisabled() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(false);
        // mockSecret 未设置
        
        // mockEnable为false时，mockSecret不是必需的
        assertDoesNotThrow(properties::validate);
        
        // 即使设置了mockSecret也应该通过验证
        properties.setMockSecret("some-secret");
        assertDoesNotThrow(properties::validate);
    }

    @Test
    void testValidateWeakPasswordRejection() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(true);
        
        // 测试常见弱密码
        String[] weakPasswords = {"123456", "password", "admin", "test", "123456789"};
        
        for (String weakPassword : weakPasswords) {
            properties.setMockSecret(weakPassword);
            IllegalStateException exception = assertThrows(IllegalStateException.class, properties::validate);
            assertTrue(exception.getMessage().contains("不能使用常见弱密码"), 
                "弱密码 '" + weakPassword + "' 应被拒绝");
        }
        
        // 测试强密码通过
        properties.setMockSecret("secure-mock-secret-" + System.currentTimeMillis());
        assertDoesNotThrow(properties::validate);
    }

    @Test
    void testOtherFieldsValidation() {
        SecurityProperties properties = new SecurityProperties();
        
        // 设置必需字段
        properties.setTokenHeader("Authorization");
        properties.setTokenTimeout(Duration.ofMinutes(30));
        properties.setTokenSecret("secure-token-secret");
        properties.setSessionTimeout(Duration.ofHours(1));
        properties.setMockEnable(false);
        
        // 验证通过
        assertDoesNotThrow(properties::validate);
    }

    @Test
    void testEmptyStringValidation() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(true);
        properties.setMockSecret("   "); // 空白字符串
        
        IllegalStateException exception = assertThrows(IllegalStateException.class, properties::validate);
        assertTrue(exception.getMessage().contains("hadoken.security.mock-secret"));
    }

    @Test
    void testNullMockEnableHandling() {
        SecurityProperties properties = new SecurityProperties();
        properties.setMockEnable(null);
        
        // null mockEnable 应视为false
        assertDoesNotThrow(properties::validate);
        
        // 即使有mockSecret也应该通过
        properties.setMockSecret("some-secret");
        assertDoesNotThrow(properties::validate);
    }
}
```

注意：
1. 创建完整的SecurityProperties测试覆盖
2. 测试默认密码移除后的行为
3. 测试条件验证（mockEnable为true时才需要mockSecret）
4. 测试弱密码拒绝逻辑
5. 测试边界情况（null、空字符串、空白字符串）
6. 测试其他字段的正常验证
7. 使用Spring Boot Test进行属性绑定测试
  </action>
  <verify>
    <automated>grep -n "@Test void testMockSecretNoDefaultValue" hadoken-security-spring-boot-starter/src/test/java/com/github/hadoken/framework/security/autoconfigure/SecurityPropertiesTest.java</automated>
  </verify>
  <done>SecurityPropertiesTest.java创建成功，包含完整的默认密码移除测试</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 更新DBAESUtil移除硬编码密钥</name>
  <files>hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java</files>
  <read_first>
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java
hadoken-common/src/main/java/com/github/hadoken/common/config/EncryptProperties.java
hadoken-common/src/main/java/com/github/hadoken/common/util/AesGcmUtil.java
  </read_first>
  <behavior>
- 测试1: 硬编码密钥"rhy"和固定IV已移除
- 测试2: 通过配置注入数据库加密密钥
- 测试3: 使用AES-GCM替代AES-CBC（或保持CBC但使用随机IV）
- 测试4: 支持加密功能禁用
- 测试5: 保持API兼容性
- 测试6: 添加日志记录
  </behavior>
  <action>
修改 hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java 文件：

```java
package com.github.hadoken.framework.mybatis.core.util;

import com.github.hadoken.common.config.EncryptProperties;
import com.github.hadoken.common.util.AesGcmUtil;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import javax.annotation.PostConstruct;
import javax.crypto.Cipher;
import javax.crypto.spec.IvParameterSpec;
import javax.crypto.spec.SecretKeySpec;
import java.security.SecureRandom;
import java.util.Base64;

/**
 * 数据库字段AES加密工具类
 * <p>
 * 用于数据库字段的透明加密解密。
 * 已升级：移除硬编码密钥和固定IV，通过EncryptProperties配置注入。
 * 建议使用AES-256-GCM模式（默认），也支持向后兼容的AES-CBC模式。
 *
 * @author yanggj
 * @version 2.0.0
 * @date 2022/8/25 10:19
 */
@Component
@Slf4j
public class DBAESUtil {

    /**
     * 加密算法模式
     */
    public enum AlgorithmMode {
        /** AES-GCM模式（推荐，提供认证加密） */
        GCM,
        /** AES-CBC模式（向后兼容） */
        CBC
    }

    @Autowired(required = false)
    private EncryptProperties encryptProperties;

    private byte[] dbEncryptionKey;
    private AlgorithmMode algorithmMode = AlgorithmMode.GCM;
    private boolean enabled = true;

    @PostConstruct
    public void init() {
        if (encryptProperties == null) {
            log.warn("EncryptProperties未配置，数据库字段加密功能将禁用");
            enabled = false;
            return;
        }

        if (!encryptProperties.isEnabled()) {
            log.info("加密功能全局禁用，数据库字段加密也将禁用");
            enabled = false;
            return;
        }

        try {
            String aesKeyBase64 = encryptProperties.getAesKey();
            if (aesKeyBase64 == null || aesKeyBase64.trim().isEmpty()) {
                throw new IllegalStateException("数据库加密密钥未配置，请设置 hadoken.security.encrypt.aes-key");
            }

            dbEncryptionKey = AesGcmUtil.decodeKeyFromBase64(aesKeyBase64);
            if (!AesGcmUtil.isValidAes256Key(dbEncryptionKey)) {
                throw new IllegalArgumentException(
                    String.format("数据库加密密钥必须为32字节（256位），当前为%d字节", 
                        dbEncryptionKey == null ? 0 : dbEncryptionKey.length)
                );
            }

            // 检查是否配置了数据库专用加密模式
            String modeConfig = System.getProperty("hadoken.db.encryption.mode", "GCM");
            try {
                algorithmMode = AlgorithmMode.valueOf(modeConfig.toUpperCase());
            } catch (IllegalArgumentException e) {
                log.warn("未知的加密模式配置: {}, 使用默认GCM模式", modeConfig);
                algorithmMode = AlgorithmMode.GCM;
            }

            log.info("数据库字段加密初始化成功，模式: {}, 启用: {}", algorithmMode, enabled);
        } catch (Exception e) {
            log.error("数据库字段加密初始化失败: {}", e.getMessage(), e);
            enabled = false;
            throw new IllegalStateException("数据库字段加密初始化失败: " + e.getMessage(), e);
        }
    }

    /**
     * 加密数据库字段
     *
     * @param content 明文内容
     * @return Base64编码的加密内容，如果加密失败或禁用则返回原文
     */
    public String encrypt(String content) {
        if (!enabled || dbEncryptionKey == null) {
            log.debug("数据库加密功能禁用，返回原文");
            return content;
        }

        if (content == null) {
            return null;
        }

        try {
            switch (algorithmMode) {
                case GCM:
                    return encryptWithGcm(content);
                case CBC:
                    return encryptWithCbc(content);
                default:
                    throw new IllegalStateException("不支持的加密模式: " + algorithmMode);
            }
        } catch (Exception e) {
            log.error("数据库字段加密失败: {}", e.getMessage(), e);
            // 加密失败时返回原文（根据业务需求决定）
            return content;
        }
    }

    /**
     * 解密数据库字段
     *
     * @param content Base64编码的加密内容
     * @return 解密后的明文内容，如果解密失败或禁用则返回原文
     */
    public String decrypt(String content) {
        if (!enabled || dbEncryptionKey == null) {
            log.debug("数据库加密功能禁用，返回原文");
            return content;
        }

        if (content == null) {
            return null;
        }

        try {
            // 尝试检测加密模式
            if (isGcmEncrypted(content)) {
                return decryptWithGcm(content);
            } else {
                // 默认为CBC模式（向后兼容）
                return decryptWithCbc(content);
            }
        } catch (Exception e) {
            log.error("数据库字段解密失败: {}", e.getMessage(), e);
            // 解密失败时返回原文（可能是未加密的数据）
            return content;
        }
    }

    /**
     * 使用AES-GCM模式加密
     */
    private String encryptWithGcm(String content) throws Exception {
        return AesGcmUtil.encrypt(content, dbEncryptionKey);
    }

    /**
     * 使用AES-GCM模式解密
     */
    private String decryptWithGcm(String content) throws Exception {
        return AesGcmUtil.decrypt(content, dbEncryptionKey);
    }

    /**
     * 使用AES-CBC模式加密（向后兼容）
     */
    private String encryptWithCbc(String content) throws Exception {
        // 生成随机IV（16字节）
        byte[] iv = new byte[16];
        SecureRandom random = new SecureRandom();
        random.nextBytes(iv);

        SecretKeySpec keySpec = new SecretKeySpec(dbEncryptionKey, "AES");
        Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");
        IvParameterSpec ivSpec = new IvParameterSpec(iv);
        cipher.init(Cipher.ENCRYPT_MODE, keySpec, ivSpec);
        
        byte[] encrypted = cipher.doFinal(content.getBytes());
        
        // 组合IV和密文：IV + 密文
        byte[] combined = new byte[iv.length + encrypted.length];
        System.arraycopy(iv, 0, combined, 0, iv.length);
        System.arraycopy(encrypted, 0, combined, iv.length, encrypted.length);
        
        return Base64.getEncoder().encodeToString(combined);
    }

    /**
     * 使用AES-CBC模式解密（向后兼容）
     */
    private String decryptWithCbc(String content) throws Exception {
        byte[] decoded = Base64.getDecoder().decode(content);
        
        // 提取IV（前16字节）和密文
        byte[] iv = new byte[16];
        byte[] encrypted = new byte[decoded.length - 16];
        System.arraycopy(decoded, 0, iv, 0, 16);
        System.arraycopy(decoded, 16, encrypted, 0, encrypted.length);

        SecretKeySpec keySpec = new SecretKeySpec(dbEncryptionKey, "AES");
        Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");
        IvParameterSpec ivSpec = new IvParameterSpec(iv);
        cipher.init(Cipher.DECRYPT_MODE, keySpec, ivSpec);
        
        byte[] original = cipher.doFinal(encrypted);
        return new String(original);
    }

    /**
     * 检测是否为GCM加密的数据
     */
    private boolean isGcmEncrypted(String content) {
        try {
            byte[] decoded = Base64.getDecoder().decode(content);
            // GCM加密的数据包含12字节IV + 密文 + 16字节认证标签
            // 简单检查：长度足够且能正常解码
            return decoded.length >= 28; // 12字节IV + 至少1字节数据 + 16字节标签
        } catch (Exception e) {
            return false;
        }
    }

    /**
     * 检查加密功能是否启用
     */
    public boolean isEnabled() {
        return enabled;
    }

    /**
     * 获取当前加密模式
     */
    public AlgorithmMode getAlgorithmMode() {
        return algorithmMode;
    }

    /**
     * 已弃用：旧版加密方法（硬编码密钥）
     * @deprecated 使用配置注入的密钥
     */
    @Deprecated(since = "2.0.0", forRemoval = true)
    public static String deprecatedEncrypt(String content) throws Exception {
        log.warn("使用已弃用的硬编码密钥加密方法，请升级到配置注入版本");
        throw new UnsupportedOperationException("硬编码密钥加密方法已弃用，请使用配置注入版本");
    }

    /**
     * 已弃用：旧版解密方法（硬编码密钥）
     * @deprecated 使用配置注入的密钥
     */
    @Deprecated(since = "2.0.0", forRemoval = true)
    public static String deprecatedDecrypt(String content) throws Exception {
        log.warn("使用已弃用的硬编码密钥解密方法，请升级到配置注入版本");
        throw new UnsupportedOperationException("硬编码密钥解密方法已弃用，请使用配置注入版本");
    }
}
```

关键修改：
1. 移除硬编码密钥 `KEY = "rhy"` 和固定IV `DEFAULT_V = "6859505890402435"`
2. 添加 `@Component` 注解，成为Spring管理的Bean
3. 通过 `@Autowired` 注入 `EncryptProperties`
4. 使用 `@PostConstruct` 初始化数据库加密密钥
5. 支持两种加密模式：GCM（推荐）和CBC（向后兼容）
6. 添加加密功能启用/禁用控制
7. 自动检测加密模式（GCM或CBC）
8. 添加日志记录
9. 标记旧版硬编码方法为已弃用
10. 提供更好的错误处理和降级策略
  </action>
  <verify>
    <automated>grep -n "private static final String KEY = \"rhy\"" hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java 2>/dev/null || echo "硬编码密钥已移除"</automated>
  </verify>
  <done>DBAESUtil.java更新完成，硬编码密钥和固定IV已移除，支持配置注入和多种加密模式</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 修复DefaultEncryptor和DefaultDecryptor异常处理</name>
  <files>
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultDecryptor.java
  </files>
  <read_first>
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultDecryptor.java
hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java
  </read_first>
  <behavior>
- 测试1: e.printStackTrace()替换为log.error()
- 测试2: 保持原有逻辑和API兼容性
- 测试3: 添加适当的异常上下文信息
- 测试4: 使用try-with-resources或适当资源管理
- 测试5: 遵循项目日志规范
  </behavior>
  <action>
修改 hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java 文件：

```java
package com.github.hadoken.framework.mybatis.core.codec;

import com.github.hadoken.framework.mybatis.core.util.DBAESUtil;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

/**
 * 默认加密器实现
 *
 * @author yanggj
 * @version 2.0.0
 * @date 2022/8/25 10:19
 */
@Component
@Slf4j
public class DefaultEncryptor implements Encryptor {

    @Autowired
    private DBAESUtil dbAESUtil;

    @Override
    public String encrypt(String plaintext) {
        if (plaintext == null) {
            return null;
        }

        try {
            return dbAESUtil.encrypt(plaintext);
        } catch (Exception e) {
            log.error("字段加密失败, plaintext: {}, error: {}", 
                maskSensitiveData(plaintext), e.getMessage(), e);
            // 根据业务需求决定：返回原文或抛出异常
            // 当前实现返回原文，避免加密失败导致业务中断
            return plaintext;
        }
    }

    /**
     * 掩码敏感数据（用于日志）
     */
    private String maskSensitiveData(String data) {
        if (data == null || data.length() <= 8) {
            return "***";
        }
        // 显示前4位和后4位，中间用*代替
        return data.substring(0, 4) + "***" + data.substring(data.length() - 4);
    }
}
```

修改 hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultDecryptor.java 文件：

```java
package com.github.hadoken.framework.mybatis.core.codec;

import com.github.hadoken.framework.mybatis.core.util.DBAESUtil;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

/**
 * 默认解密器实现
 *
 * @author yanggj
 * @version 2.0.0
 * @date 2022/8/25 10:19
 */
@Component
@Slf4j
public class DefaultDecryptor implements Decryptor {

    @Autowired
    private DBAESUtil dbAESUtil;

    @Override
    public String decrypt(String ciphertext) {
        if (ciphertext == null) {
            return null;
        }

        try {
            return dbAESUtil.decrypt(ciphertext);
        } catch (Exception e) {
            log.error("字段解密失败, ciphertext: {}, error: {}", 
                maskSensitiveData(ciphertext), e.getMessage(), e);
            // 根据业务需求决定：返回原文或抛出异常
            // 当前实现返回原文，可能是未加密的数据
            return ciphertext;
        }
    }

    /**
     * 掩码敏感数据（用于日志）
     */
    private String maskSensitiveData(String data) {
        if (data == null || data.length() <= 8) {
            return "***";
        }
        // 显示前4位和后4位，中间用*代替
        return data.substring(0, 4) + "***" + data.substring(data.length() - 4);
    }
}
```

关键修改：
1. 添加 `@Slf4j` 注解，自动生成log字段
2. 替换 `e.printStackTrace()` 为 `log.error()`
3. 在日志中包含适当的上下文信息（掩码后的数据）
4. 添加掩码敏感数据的方法，避免在日志中暴露明文或密文
5. 保持原有业务逻辑：加密/解密失败时返回原文
6. 添加版本号更新到2.0.0
7. 遵循项目异常处理模式：记录错误但不中断业务
8. 使用 `@Autowired` 注入 `DBAESUtil`
9. 添加 `@Component` 注解（如果原来没有）
  </action>
  <verify>
    <automated>grep -n "e.printStackTrace()" hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/DefaultEncryptor.java 2>/dev/null || echo "e.printStackTrace()已移除"</automated>
  </verify>
  <done>DefaultEncryptor和DefaultDecryptor更新完成，异常处理改进，e.printStackTrace()替换为log.error()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 默认密码边界 | 配置默认值 → 必需显式配置 |
| 数据库加密边界 | 硬编码密钥 → 配置注入密钥 |
| 异常日志边界 | 控制台打印 → 结构化日志 |
| 敏感数据边界 | 日志明文 → 掩码处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Spoofing | 默认密码"123456" | mitigate | 移除默认值，强制显式配置 |
| T-02-04-02 | Information Disclosure | 硬编码数据库密钥 | mitigate | 密钥配置化，无硬编码 |
| T-02-04-03 | Tampering | 固定IV攻击 | mitigate | 使用随机IV（GCM/CBC） |
| T-02-04-04 | Repudiation | 异常堆栈打印 | mitigate | 使用结构化日志记录 |
| T-02-04-05 | Information Disclosure | 日志敏感数据 | mitigate | 敏感数据掩码处理 |
| T-02-04-06 | Denial of Service | 加密失败处理 | accept | 加密失败时返回原文，业务继续 |

## 安全控制
1. **密码安全**: 移除所有默认密码，强制显式配置
2. **密钥管理**: 数据库加密密钥外部化配置
3. **加密安全**: 使用随机IV，支持GCM认证加密
4. **日志安全**: 结构化日志，敏感数据掩码
5. **异常安全**: 适当错误处理，避免信息泄露
6. **向后兼容**: 支持多种加密模式，平滑迁移
7. **配置验证**: 必需配置检查，弱密码拒绝
</threat_model>

<verification>
1. 运行SecurityProperties测试: `cd hadoken-security-spring-boot-starter && mvn test -Dtest=SecurityPropertiesTest`
2. 验证默认密码移除: `grep -n "\"123456\"" hadoken-security-spring-boot-starter/src/main/java/com/github/hadoken/framework/security/autoconfigure/SecurityProperties.java`
3. 验证硬编码密钥移除: `grep -n "\"rhy\"" hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/util/DBAESUtil.java`
4. 验证异常处理改进: `grep -n "e.printStackTrace()" hadoken-mybatis-spring-boot-starter/src/main/java/com/github/hadoken/framework/mybatis/core/codec/*.java`
5. 检查编译: `cd hadoken-mybatis-spring-boot-starter && mvn compile`
6. 测试数据库加密功能: 创建简单测试验证DBAESUtil正常工作
7. 验证配置注入: 检查DBAESUtil是否正确注入EncryptProperties
</verification>

<success_criteria>
1. [ ] SecurityProperties中的默认密码"123456"完全移除
2. [ ] mockSecret字段无默认值，必需显式配置
3. [ ] 添加validate()方法验证配置完整性
4. [ ] 创建SecurityPropertiesTest测试文件
5. [ ] DBAESUtil中的硬编码密钥"rhy"和固定IV移除
6. [ ] 数据库加密密钥通过EncryptProperties配置注入
7. [ ] 支持AES-GCM和AES-CBC两种加密模式
8. [ ] DefaultEncryptor/DefaultDecryptor中的e.printStackTrace()替换为log.error()
9. [ ] 添加敏感数据掩码处理，避免日志泄露
10. [ ] 所有文件编译通过，测试覆盖关键场景
11. [ ] 符合项目代码规范和文档标准
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-fixes/02-04-SUMMARY.md`
</output>