---
phase: 03-core-framework-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - .planning/phases/03-core-framework-upgrade/03-03-SUMMARY.md
  - .planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
user_setup: []

must_haves:
  truths:
    - "核心框架升级验证完成，所有 CORE 需求得到验证"
    - "Spring Boot 3.5.x、Spring Cloud 2025.x、Spring Cloud Alibaba 兼容性得到确认"
    - "项目在 JDK 25 环境下能够成功编译和运行测试"
    - "发现的任何兼容性问题都有明确的记录和解决方案"
  artifacts:
    - path: ".planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md"
      provides: "核心框架升级验证报告"
      min_lines: 50
    - path: "src/test/java/com/github/hadoken/framework/upgrade/FrameworkCompatibilityIntegrationTest.java"
      provides: "框架兼容性集成测试"
      min_lines: 40
    - path: ".planning/phases/03-core-framework-upgrade/NOTES.md"
      provides: "升级过程中的问题和解决方案记录"
      contains: "版本兼容性状态"
  key_links:
    - from: "VERIFICATION-REPORT.md"
      to: "03-01-SUMMARY.md 和 03-02-SUMMARY.md"
      via: "验证结果汇总"
      pattern: "引用前两个计划的结果"
    - from: "FrameworkCompatibilityIntegrationTest.java"
      to: "所有其他测试类"
      via: "集成测试验证"
      pattern: "综合测试所有升级组件"
---

<objective>
执行全面的兼容性验证测试，生成核心框架升级验证报告，确认 Spring Boot 3.5.x、Spring Cloud 2025.x 和 Spring Cloud Alibaba 在 JDK 25 环境下的兼容性。

Purpose: 验证前两个计划的结果，确保整个核心框架升级成功，所有 CORE 需求得到满足，为后续阶段提供稳定的基础。
Output: 
- 框架兼容性集成测试
- 核心框架升级验证报告
- 所有 CORE 需求的验证状态
- 下一步建议和风险提示
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 前两个计划的结果
@.planning/phases/03-core-framework-upgrade/03-01-SUMMARY.md (依赖)
@.planning/phases/03-core-framework-upgrade/03-02-SUMMARY.md (依赖)

# 用户决策
@.planning/phases/03-core-framework-upgrade/03-CONTEXT.md

# 研究结果
@.planning/phases/03-core-framework-upgrade/03-RESEARCH.md

# JDK 25 代码规范
@.planning/docs/JDK25-CODE-STANDARDS.md

# Phase 3 成功标准
根据 ROADMAP.md，Phase 3 的成功标准：
1. Spring Boot 已升级到 3.5.x 或支持 JDK 25 的最新稳定版本
2. Spring Cloud 版本与 Spring Boot 兼容，微服务功能正常
3. Spring Cloud Alibaba 版本与 Spring Cloud 兼容
4. Spring Framework 和 Jakarta EE 版本通过 Spring Boot BOM 正确管理
5. 核心框架启动成功，无兼容性错误
</context>

<tasks>

<task type="auto">
  <name>任务 1: 收集和分析前两个计划的结果</name>
  <files>
    .planning/phases/03-core-framework-upgrade/03-01-SUMMARY.md
    .planning/phases/03-core-framework-upgrade/03-02-SUMMARY.md
    .planning/phases/03-core-framework-upgrade/NOTES.md
  </files>
  <action>
1. 读取 03-01-SUMMARY.md，提取关键信息：
   - 实际使用的 Spring Boot 版本号
   - Spring Boot 3.5.x 升级结果（成功/失败/部分成功）
   - Spring Framework 版本验证结果
   - Jakarta EE 版本验证结果
   - 遇到的任何问题和解决方案

2. 读取 03-02-SUMMARY.md，提取关键信息：
   - 实际使用的 Spring Cloud 版本号
   - 实际使用的 Spring Cloud Alibaba 版本号
   - Spring Cloud 兼容性验证结果
   - Spring Cloud Alibaba 兼容性验证结果
   - 遇到的任何兼容性问题和解决方案

3. 读取 NOTES.md，提取升级过程中的记录：
   - 版本选择的原因
   - 遇到的技术问题
   - 测试结果和发现

4. 分析汇总信息，确定：
   - 哪些成功标准已满足
   - 哪些成功标准未满足或需要进一步验证
   - 存在的风险和不确定性
   - 需要的进一步测试

5. 将分析结果记录到临时文件 analysis.txt
  </action>
  <verify>
    分析结果记录在 analysis.txt 中，包含前两个计划的关键结果总结
  </verify>
  <done>前两个计划的结果已收集和分析，确定了验证的重点和潜在问题</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2: 创建框架兼容性集成测试</name>
  <files>
    src/test/java/com/github/hadoken/framework/upgrade/FrameworkCompatibilityIntegrationTest.java
  </files>
  <behavior>
    - 测试 1: 验证所有核心框架组件的版本兼容性
    - 测试 2: 验证微服务基本功能可用性
    - 测试 3: 验证项目能够在 JDK 25 环境下启动和运行
    - 测试 4: 验证 Spring Boot BOM 管理的依赖版本一致性
  </behavior>
  <action>
1. 创建 FrameworkCompatibilityIntegrationTest.java：
   ```java
   package com.github.hadoken.framework.upgrade;
   
   import org.junit.Test;
   import org.junit.runner.RunWith;
   import org.springframework.boot.SpringBootVersion;
   import org.springframework.boot.test.context.SpringBootTest;
   import org.springframework.test.context.junit4.SpringRunner;
   
   import org.springframework.cloud.client.ServiceInstance;
   import org.springframework.cloud.client.loadbalancer.LoadBalancerClient;
   import org.springframework.cloud.context.config.annotation.RefreshScope;
   
   import jakarta.servlet.http.HttpServlet;
   import jakarta.ws.rs.core.Application;
   
   import java.lang.annotation.Annotation;
   
   import static org.junit.Assert.assertNotNull;
   import static org.junit.Assert.assertTrue;
   
   @RunWith(SpringRunner.class)
   @SpringBootTest
   public class FrameworkCompatibilityIntegrationTest {
       
       @Test
       public void testCompleteFrameworkCompatibility() {
           // 1. 验证 Spring Boot 版本
           String springBootVersion = SpringBootVersion.getVersion();
           assertNotNull("Spring Boot version should not be null", springBootVersion);
           assertTrue("Spring Boot should be 3.5.x, got: " + springBootVersion,
               springBootVersion.startsWith("3.5."));
           
           System.out.println("✅ Spring Boot Version: " + springBootVersion);
           
           // 2. 验证 Spring Framework 版本
           String springVersion = org.springframework.core.SpringVersion.getVersion();
           assertNotNull("Spring Framework version should not be null", springVersion);
           System.out.println("✅ Spring Framework Version: " + springVersion);
           
           // 3. 验证 Jakarta EE 版本
           Package servletPackage = HttpServlet.class.getPackage();
           String servletSpecVersion = servletPackage.getSpecificationVersion();
           assertTrue("Jakarta Servlet should be 5.0+, got: " + servletSpecVersion,
               servletSpecVersion != null && servletSpecVersion.startsWith("5"));
           
           Package rsPackage = Application.class.getPackage();
           String rsSpecVersion = rsPackage.getSpecificationVersion();
           assertTrue("Jakarta RS should be 3.1+, got: " + rsSpecVersion,
               rsSpecVersion != null && rsSpecVersion.startsWith("3"));
           
           System.out.println("✅ Jakarta Servlet API: " + servletSpecVersion);
           System.out.println("✅ Jakarta RS API: " + rsSpecVersion);
           
           // 4. 验证 Spring Cloud 可用性
           Class<?> loadBalancerClientClass = LoadBalancerClient.class;
           Class<?> serviceInstanceClass = ServiceInstance.class;
           assertNotNull("LoadBalancerClient should be available", loadBalancerClientClass);
           assertNotNull("ServiceInstance should be available", serviceInstanceClass);
           
           Package cloudPackage = LoadBalancerClient.class.getPackage();
           String cloudImplVersion = cloudPackage.getImplementationVersion();
           if (cloudImplVersion != null) {
               assertTrue("Spring Cloud should be 2025.x, got: " + cloudImplVersion,
                   cloudImplVersion.startsWith("2025"));
               System.out.println("✅ Spring Cloud Version: " + cloudImplVersion);
           }
           
           // 5. 验证 Spring Cloud Alibaba 可用性（如果配置）
           try {
               Class<?> nacosClass = Class.forName("com.alibaba.cloud.nacos.NacosDiscoveryProperties");
               assertNotNull("NacosDiscoveryProperties should be available if Alibaba is configured", nacosClass);
               System.out.println("✅ Spring Cloud Alibaba is available");
               
               Package alibabaPackage = nacosClass.getPackage();
               String alibabaImplVersion = alibabaPackage.getImplementationVersion();
               if (alibabaImplVersion != null) {
                   System.out.println("✅ Spring Cloud Alibaba Version: " + alibabaImplVersion);
               }
           } catch (ClassNotFoundException e) {
               System.out.println("⚠️ Spring Cloud Alibaba not configured or not compatible");
           }
           
           // 6. 验证 Spring Cloud 注解
           Class<? extends Annotation> refreshScopeClass = RefreshScope.class;
           assertNotNull("@RefreshScope should be available", refreshScopeClass);
           System.out.println("✅ Spring Cloud annotations are available");
       }
       
       @Test
       public void testDependencyConsistency() {
           // 验证 BOM 管理的依赖版本一致性
           System.out.println("=== Dependency Consistency Check ===");
           
           // 检查 Spring Boot 相关依赖
           checkPackageVersion("org.springframework.boot", "Spring Boot");
           checkPackageVersion("org.springframework", "Spring Framework");
           checkPackageVersion("org.springframework.cloud", "Spring Cloud");
           
           // 检查 Jakarta EE 相关依赖
           checkPackageVersion("jakarta.servlet", "Jakarta Servlet");
           checkPackageVersion("jakarta.ws.rs", "Jakarta RS");
           
           System.out.println("✅ Dependency consistency check completed");
       }
       
       private void checkPackageVersion(String packageName, String displayName) {
           try {
               Package pkg = Package.getPackage(packageName);
               if (pkg != null) {
                   String implVersion = pkg.getImplementationVersion();
                   String specVersion = pkg.getSpecificationVersion();
                   
                   System.out.println(displayName + " - Implementation: " + 
                                     (implVersion != null ? implVersion : "N/A") +
                                     ", Specification: " + 
                                     (specVersion != null ? specVersion : "N/A"));
               } else {
                   System.out.println("⚠️ " + displayName + " package not found in classpath");
               }
           } catch (Exception e) {
               System.out.println("⚠️ Error checking " + displayName + " version: " + e.getMessage());
           }
       }
       
       @Test
       public void testContextLoads() {
           // 简单的上下文加载测试
           // 如果这个测试通过，说明基本的 Spring Boot 上下文可以加载
           System.out.println("✅ Spring Boot context loads successfully");
       }
   }
   ```

2. 创建测试配置文件（如果需要）：
   ```bash
   mkdir -p src/test/resources/
   cat > src/test/resources/application-test.yml << 'EOF'
   # 测试配置
   spring:
     application:
       name: hadoken-framework-upgrade-test
   
   # 禁用不需要的功能以加快测试速度
   spring.cloud:
     enabled: false
   
   logging:
     level:
       com.github.hadoken: DEBUG
   EOF
   ```

3. 运行集成测试：
   ```bash
   mvn test -Dtest=FrameworkCompatibilityIntegrationTest
   ```
  </action>
  <verify>
    <automated>mvn test -Dtest=FrameworkCompatibilityIntegrationTest</automated>
  </verify>
  <done>
    框架兼容性集成测试已创建并运行，验证了所有核心框架组件的兼容性，
    测试通过或提供了明确的兼容性状态报告。
  </done>
</task>

<task type="auto">
  <name>任务 3: 运行全面测试套件</name>
  <files>N/A（Maven命令）</files>
  <action>
1. 运行所有升级相关的测试：
   ```bash
   mvn test -Dtest="*upgrade.*Test"
   ```

2. 如果存在测试失败，分析原因：
   - 是版本兼容性问题还是测试代码问题？
   - 是否需要调整测试配置或代码？
   - 是否是预期的兼容性问题（如 Spring Cloud Alibaba 不兼容）？

3. 运行完整的项目测试套件（跳过可能失败的其他模块测试）：
   ```bash
   # 首先测试 hadoken-common 模块（核心模块）
   cd hadoken-common && mvn test
   cd ..
   ```

4. 如果 hadoken-common 测试失败：
   - 分析失败原因
   - 确定是否与核心框架升级相关
   - 记录问题和可能的解决方案

5. 记录测试结果到 test-results.txt：
   ```bash
   mvn test -Dtest="*upgrade.*Test" 2>&1 | tee test-results.txt
   ```
  </action>
  <verify>
    <automated>mvn test -Dtest="*upgrade.*Test" 2>&1 | grep -E "Tests run:|BUILD"</automated>
  </verify>
  <done>
    所有升级相关测试已运行，测试结果已记录。
    hadoken-common 模块的基本测试已运行，验证了核心功能。
  </done>
</task>

<task type="auto">
  <name>任务 4: 生成核心框架升级验证报告</name>
  <files>.planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md</files>
  <action>
1. 创建 VERIFICATION-REPORT.md：
   ```markdown
   # Phase 3: Core Framework Upgrade - 验证报告
   
   **生成日期:** $(date)
   **Phase:** 03-core-framework-upgrade
   **验证完成:** $(date +%Y-%m-%d)
   
   ## 执行摘要
   
   | 组件 | 目标版本 | 实际版本 | 兼容性状态 | 备注 |
   |------|----------|----------|------------|------|
   | Spring Boot | 3.5.x | {实际版本} | {成功/失败/部分成功} | {备注} |
   | Spring Cloud | 2025.0.x | {实际版本} | {成功/失败/部分成功} | {备注} |
   | Spring Cloud Alibaba | 2023.0.1.0+ | {实际版本} | {成功/失败/不适用} | {备注} |
   | Spring Framework | 通过 BOM 管理 | {实际版本} | {成功} | 通过 Spring Boot BOM 管理 |
   | Jakarta EE | 通过 BOM 管理 | {实际版本} | {成功} | 通过 Spring Boot BOM 管理 |
   
   ## Phase 3 成功标准验证
   
   ### 1. Spring Boot 已升级到 3.5.x 或支持 JDK 25 的最新稳定版本
   - **状态:** {通过/失败}
   - **验证方法:** SpringBootVersionTest, FrameworkCompatibilityIntegrationTest
   - **实际版本:** {版本号}
   - **JDK 25 支持:** {确认/未确认}
   - **备注:** {任何问题或注意事项}
   
   ### 2. Spring Cloud 版本与 Spring Boot 兼容，微服务功能正常
   - **状态:** {通过/失败}
   - **验证方法:** SpringCloudCompatibilityTest, FrameworkCompatibilityIntegrationTest
   - **实际版本:** {版本号}
   - **兼容性确认:** {是/否}
   - **备注:** {任何兼容性问题}
   
   ### 3. Spring Cloud Alibaba 版本与 Spring Cloud 兼容
   - **状态:** {通过/失败/不适用}
   - **验证方法:** AlibabaCompatibilityTest, FrameworkCompatibilityIntegrationTest
   - **实际版本:** {版本号 或 "未配置"}
   - **兼容性确认:** {是/否/未测试}
   - **备注:** {兼容性问题或配置状态}
   
   ### 4. Spring Framework 和 Jakarta EE 版本通过 Spring Boot BOM 正确管理
   - **状态:** {通过}
   - **验证方法:** SpringFrameworkVersionTest, JakartaEEVersionTest, FrameworkCompatibilityIntegrationTest
   - **Spring Framework 版本:** {版本号}
   - **Jakarta EE API 版本:** Servlet {版本}, RS {版本}
   - **BOM 管理验证:** 通过 Spring Boot BOM 自动管理
   
   ### 5. 核心框架启动成功，无兼容性错误
   - **状态:** {通过/失败}
   - **验证方法:** 编译测试、集成测试
   - **编译状态:** {成功/失败}
   - **测试通过率:** {通过数/总数}
   - **启动验证:** {成功/失败}
   
   ## 详细测试结果
   
   ### 测试套件执行结果
   ```
   {从 test-results.txt 提取的测试执行摘要}
   ```
   
   ### 关键测试通过情况
   - [ ] SpringBootVersionTest - {通过/失败}
   - [ ] SpringFrameworkVersionTest - {通过/失败}
   - [ ] JakartaEEVersionTest - {通过/失败}
   - [ ] SpringCloudCompatibilityTest - {通过/失败}
   - [ ] AlibabaCompatibilityTest - {通过/失败}
   - [ ] FrameworkCompatibilityIntegrationTest - {通过/失败}
   
   ## 发现的问题和解决方案
   
   ### 问题 1: {问题描述}
   - **影响:** {影响范围}
   - **根本原因:** {原因分析}
   - **解决方案:** {采取的解决方案}
   - **状态:** {已解决/未解决/缓解}
   
   ### 问题 2: {问题描述}
   - **影响:** {影响范围}
   - **根本原因:** {原因分析}
   - **解决方案:** {采取的解决方案}
   - **状态:** {已解决/未解决/缓解}
   
   ## 兼容性风险分析
   
   ### 已知风险
   1. **Spring Cloud Alibaba 兼容性风险**
      - **风险等级:** {高/中/低}
      - **影响:** {对微服务功能的影响}
      - **缓解措施:** {已采取的措施}
   
   2. **传递依赖版本冲突风险**
      - **风险等级:** {高/中/低}
      - **影响:** {对构建或运行时的影响}
      - **缓解措施:** {已采取的措施}
   
   ### 未知风险
   - {列出需要进一步验证的潜在风险}
   
   ## 建议和下一步
   
   ### 立即建议
   1. {针对发现问题的建议}
   2. {针对风险缓解的建议}
   
   ### 后续阶段注意事项
   1. **Phase 4 (数据库依赖升级):** {注意事项}
   2. **Phase 5 (工具库升级):** {注意事项}
   3. **Phase 6+ (其他阶段):** {注意事项}
   
   ## 附录
   
   ### A. 实际版本信息
   - Spring Boot: {版本}
   - Spring Cloud: {版本}
   - Spring Cloud Alibaba: {版本 或 "未使用"}
   - Spring Framework: {版本}
   - Jakarta Servlet API: {版本}
   - Jakarta RS API: {版本}
   
   ### B. 测试环境信息
   - JDK 版本: {java -version 输出}
   - Maven 版本: {mvn -v 输出}
   - 操作系统: {uname -a 输出}
   
   ### C. 相关文档
   - Phase 3 CONTEXT: 03-CONTEXT.md
   - Phase 3 RESEARCH: 03-RESEARCH.md
   - Plan 01 SUMMARY: 03-01-SUMMARY.md
   - Plan 02 SUMMARY: 03-02-SUMMARY.md
   - JDK 25 代码规范: JDK25-CODE-STANDARDS.md
   ```
   
   2. 使用从任务 1-3 收集的信息填充报告模板：
      - 从 analysis.txt 获取版本信息和状态
      - 从 test-results.txt 获取测试结果
      - 从 NOTES.md 获取问题和解决方案
   
   3. 确保报告准确反映实际情况，不掩盖问题
   
   4. 保存报告
  </action>
  <verify>
    VERIFICATION-REPORT.md 文件已创建，包含完整的验证信息和报告
  </verify>
  <done>
    核心框架升级验证报告已生成，包含：
    - 所有组件的版本和兼容性状态
    - Phase 3 成功标准的验证结果
    - 详细测试结果
    - 发现的问题和解决方案
    - 风险分析和建议
  </done>
</task>

<task type="auto">
  <name>任务 5: 验证 Phase 3 成功标准并更新 ROADMAP</name>
  <files>
    .planning/ROADMAP.md
    .planning/phases/03-core-framework-upgrade/03-03-SUMMARY.md
  </files>
  <action>
1. 根据 VERIFICATION-REPORT.md 的结果，评估 Phase 3 的成功标准：
   - 对照 ROADMAP.md 中的 5 条成功标准
   - 对每条标准给出明确的评估结果（通过/失败/部分通过）
   - 记录评估依据

2. 如果所有成功标准都通过：
   - 在 ROADMAP.md 中标记 Phase 3 为完成
   - 更新 Phase 3 的 Plans 部分，添加实际创建的 3 个计划

3. 如果有成功标准未通过：
   - 分析原因和影响
   - 确定是否需要额外的计划来解决问题
   - 记录在 SUMMARY.md 中

4. 创建 03-03-SUMMARY.md：
   ```markdown
   # Phase 3 Plan 03: Compatibility Verification - 总结
   
   **执行日期:** $(date)
   **计划:** 03-03-PLAN.md
   **需求覆盖:** CORE-01, CORE-02, CORE-03, CORE-04, CORE-05
   
   ## 执行结果
   
   ### 关键成果
   1. 创建并运行了 FrameworkCompatibilityIntegrationTest
   2. 生成了 VERIFICATION-REPORT.md 验证报告
   3. 运行了所有升级相关测试
   4. 评估了 Phase 3 成功标准
   
   ### 测试结果摘要
   - 升级相关测试总数: {数量}
   - 通过数: {数量}
   - 失败数: {数量}
   - 通过率: {百分比}
   
   ### Phase 3 成功标准评估
   
   | 成功标准 | 评估结果 | 依据 |
   |----------|----------|------|
   | 1. Spring Boot 升级到 3.5.x | {通过/失败} | {依据} |
   | 2. Spring Cloud 版本兼容 | {通过/失败} | {依据} |
   | 3. Spring Cloud Alibaba 兼容 | {通过/失败/不适用} | {依据} |
   | 4. Spring Framework/Jakarta EE 通过 BOM 管理 | {通过/失败} | {依据} |
   | 5. 核心框架启动成功，无兼容性错误 | {通过/失败} | {依据} |
   
   ## 发现和问题
   
   ### 主要发现
   - {发现 1}
   - {发现 2}
   
   ### 未解决问题
   - {问题 1 及影响}
   - {问题 2 及影响}
   
   ## 建议
   
   ### 对当前阶段
   - {建议 1}
   - {建议 2}
   
   ### 对后续阶段
   - **Phase 4 (数据库依赖):** {建议}
   - **Phase 5 (工具库):** {建议}
   
   ## 产出物
   
   1. `VERIFICATION-REPORT.md` - 完整的验证报告
   2. `FrameworkCompatibilityIntegrationTest.java` - 集成测试
   3. 更新的 `ROADMAP.md` - Phase 3 状态更新
   
   ## 下一步
   
   如果 Phase 3 成功标准全部满足：
   - 执行 `/gsd-execute-phase 04-database-redis-dependencies`
   
   如果存在未解决的问题：
   - {下一步行动建议}
   ```
   
   5. 更新 ROADMAP.md 中的 Phase 3 部分：
      - 更新 Plans 数量为 3
      - 添加具体的计划列表
      - 如果完成，添加完成标记
  </action>
  <verify>
    03-03-SUMMARY.md 已创建，ROADMAP.md 已根据验证结果更新
  </verify>
  <done>
    Phase 3 的验证工作完成，成功标准已评估，
    ROADMAP.md 已更新，下一步行动已明确。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | Description |
|----------|-------------|
| 测试环境 → 生产环境 | 测试通过的兼容性可能在生产环境中表现不同 |
| 验证报告 → 实际状态 | 报告可能无法完全反映所有潜在问题 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | 缓解方案 |
|-----------|----------|-----------|-------------|----------|
| T-03-13 | Tampering | 验证报告 | mitigate | 基于自动化测试结果生成报告，减少人为错误 |
| T-03-14 | Spoofing | 测试结果 | mitigate | 使用 Maven 标准测试框架，结果可重复验证 |
| T-03-15 | Repudiation | 成功标准评估 | mitigate | 明确记录评估依据和测试结果 |
| T-03-16 | Information Disclosure | 测试中的配置信息 | accept | 测试配置不包含敏感生产信息 |
| T-03-17 | Denial of Service | 测试执行失败 | mitigate | 提供详细的错误信息和诊断方法 |
| T-03-18 | Elevation of Privilege | 测试框架漏洞 | accept | 使用标准 Spring Boot Test，风险较低 |

</threat_model>

<verification>
1. **报告验证**：VERIFICATION-REPORT.md 包含完整的验证信息
2. **测试验证**：所有升级相关测试已运行，结果记录在报告中
3. **标准验证**：Phase 3 成功标准已对照验证报告进行评估
4. **文档验证**：ROADMAP.md 已根据结果更新

**最终验证命令：**
```bash
# 检查所有产出物
ls -la .planning/phases/03-core-framework-upgrade/*.md
ls -la src/test/java/com/github/hadoken/framework/upgrade/*.java

# 验证报告完整性
grep -c "执行摘要" .planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md
grep -c "成功标准验证" .planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md
```

**Phase 3 完成状态检查：**
根据 VERIFICATION-REPORT.md 中的评估，确认 Phase 3 是否成功完成。
</verification>

<success_criteria>
1. ✅ VERIFICATION-REPORT.md 已生成，包含完整的验证信息
2. ✅ FrameworkCompatibilityIntegrationTest.java 已创建并运行
3. ✅ 所有升级相关测试已运行，结果记录在报告中
4. ✅ Phase 3 的 5 条成功标准已对照验证报告进行评估
5. ✅ 03-03-SUMMARY.md 已创建，总结验证结果
6. ✅ ROADMAP.md 已根据验证结果更新
7. ✅ 下一步行动已明确（继续 Phase 4 或解决未完成问题）
</success_criteria>

<output>
完成此计划后，创建以下文件：
1. `.planning/phases/03-core-framework-upgrade/03-03-SUMMARY.md` - 计划 03 总结
2. `.planning/phases/03-core-framework-upgrade/VERIFICATION-REPORT.md` - 验证报告
3. 更新的 `.planning/ROADMAP.md` - Phase 3 状态更新

Phase 3 完成后，用户应能：
- 查看完整的验证报告，了解核心框架升级状态
- 知道是否所有成功标准都已满足
- 明确下一步行动（继续 Phase 4 或解决遗留问题）
</output>