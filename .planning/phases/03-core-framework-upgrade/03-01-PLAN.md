---
phase: 03-core-framework-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hadoken-dependencies/pom.xml
  - pom.xml
  - .planning/phases/03-core-framework-upgrade/03-01-SUMMARY.md
autonomous: true
requirements: [CORE-01, CORE-04, CORE-05]
user_setup: []

must_haves:
  truths:
    - "Spring Boot 已升级到 3.5.x（支持 JDK 25 的最新稳定版本）"
    - "项目能够在 JDK 25 环境下使用 Spring Boot 3.5.x 成功编译"
    - "Spring Framework 版本通过 Spring Boot BOM 自动管理"
    - "Jakarta EE 版本通过 Spring Boot BOM 自动管理"
  artifacts:
    - path: "hadoken-dependencies/pom.xml"
      provides: "Spring Boot 版本升级到 3.5.x"
      contains: "<spring-boot.version>3.5.x</spring-boot.version>"
    - path: "pom.xml"
      provides: "根POM继承 Spring Boot Parent 3.5.x"
      contains: "<spring-boot.version>3.5.x</spring-boot.version>"
    - path: "src/test/java/com/github/hadoken/framework/upgrade/SpringBootVersionTest.java"
      provides: "Spring Boot 版本升级验证测试"
      min_lines: 30
  key_links:
    - from: "hadoken-dependencies/pom.xml"
      to: "pom.xml"
      via: "dependencyManagement import"
      pattern: "dependencyManagement.*spring-boot-dependencies.*3.5"
    - from: "所有子模块的pom.xml"
      to: "hadoken-dependencies/pom.xml"
      via: "父POM版本继承"
      pattern: "parent.*1.0.0"
---

<objective>
验证并升级 Spring Boot 到 3.5.x（支持 JDK 25 的最新稳定版本），确保 Spring Framework 和 Jakarta EE 版本通过 Spring Boot BOM 正确管理。

Purpose: 这是核心框架升级的基础，Spring Boot 版本决定整个 Spring 生态系统的兼容性链条。
Output: 
- hadoken-dependencies/pom.xml 更新 Spring Boot 版本属性
- 根 pom.xml 验证 Spring Boot Parent 版本继承
- Spring Boot 版本验证测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 用户决策
@.planning/phases/03-core-framework-upgrade/03-CONTEXT.md

# 研究结果
@.planning/phases/03-core-framework-upgrade/03-RESEARCH.md

# 当前版本状态
@hadoken-dependencies/pom.xml
@pom.xml

# JDK 25 代码规范
@.planning/docs/JDK25-CODE-STANDARDS.md

# 现有项目结构和依赖管理
1. 根 pom.xml 继承 spring-boot-starter-parent（当前 3.3.13）
2. hadoken-dependencies/pom.xml 作为 BOM 管理所有依赖版本
3. 所有子模块通过 dependencyManagement 导入 hadoken-dependencies BOM
4. 当前 Spring Boot 版本: 3.3.13，需要升级到 3.5.x
</context>

<tasks>

<task type="auto">
  <name>任务 1: 验证 Spring Boot 3.5.x 可用版本</name>
  <files>N/A（检查外部资源）</files>
  <action>
1. 检查 Spring Boot 官方发布信息，确认支持 JDK 25 的最新稳定版本
   - 优先检查 Spring Boot 3.5.x 系列（如 3.5.0, 3.5.1, 3.5.2）
   - 确认该版本正式支持 JDK 25
   - 记录最新稳定版本号

2. 如果无法通过网络验证，采用保守策略：
   - 使用 3.5.0（假设这是支持 JDK 25 的最低版本）
   - 在任务 3 的测试中验证兼容性

3. 确定版本号后，记录在 NOTES.md 中：
   - 版本号：如 3.5.0 或 3.5.1
   - 发布日期：如果可获取
   - JDK 25 支持状态：确认信息
  </action>
  <verify>
    版本信息记录在 .planning/phases/03-core-framework-upgrade/NOTES.md 中
  </verify>
  <done>确定了 Spring Boot 3.5.x 的具体版本号用于升级</done>
</task>

<task type="auto">
  <name>任务 2: 升级 hadoken-dependencies 中的 Spring Boot 版本</name>
  <files>hadoken-dependencies/pom.xml</files>
  <action>
1. 打开 hadoken-dependencies/pom.xml

2. 在 properties 部分找到 spring-boot.version 属性（当前值为 3.3.13）

3. 将其更新为任务 1 确定的版本号，例如：
   ```xml
   <spring-boot.version>3.5.0</spring-boot.version>
   ```

4. 确认并保存更改

注意事项：
- 仅修改 spring-boot.version 属性，其他依赖版本暂时保持不变
- 确保版本号格式正确（x.y.z，无尾随空格）
- 如果使用保守策略（3.5.0），请添加注释说明这是初始尝试
  </action>
  <verify>
    <automated>grep -n "spring-boot.version" hadoken-dependencies/pom.xml | grep -E "3\.5\.[0-9]+"</automated>
  </verify>
  <done>hadoken-dependencies/pom.xml 中的 Spring Boot 版本已更新为 3.5.x</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3: 创建 Spring Boot 版本验证测试</name>
  <files>
    src/test/java/com/github/hadoken/framework/upgrade/SpringBootVersionTest.java
    src/test/java/com/github/hadoken/framework/upgrade/SpringFrameworkVersionTest.java
    src/test/java/com/github/hadoken/framework/upgrade/JakartaEEVersionTest.java
  </files>
  <behavior>
    - 测试 1: SpringBootVersionTest 验证 Spring Boot 版本为 3.5.x
    - 测试 2: SpringFrameworkVersionTest 验证 Spring Framework 版本与 Spring Boot 兼容
    - 测试 3: JakartaEEVersionTest 验证 Jakarta EE API 版本与 Spring Boot 兼容
  </behavior>
  <action>
1. 创建测试目录（如果不存在）：
   ```bash
   mkdir -p src/test/java/com/github/hadoken/framework/upgrade/
   ```

2. 创建 SpringBootVersionTest.java：
   ```java
   package com.github.hadoken.framework.upgrade;
   
   import org.junit.Test;
   import org.springframework.boot.SpringBootVersion;
   import org.springframework.core.SpringVersion;
   
   import static org.junit.Assert.assertTrue;
   import static org.junit.Assert.assertNotNull;
   
   public class SpringBootVersionTest {
       
       @Test
       public void testSpringBootVersion() {
           String version = SpringBootVersion.getVersion();
           assertNotNull("Spring Boot version should not be null", version);
           System.out.println("Spring Boot Version: " + version);
           
           // 验证版本为 3.5.x
           assertTrue("Spring Boot version should be 3.5.x, got: " + version,
               version.startsWith("3.5."));
       }
       
       @Test
       public void testSpringFrameworkVersion() {
           String version = SpringVersion.getVersion();
           assertNotNull("Spring Framework version should not be null", version);
           System.out.println("Spring Framework Version: " + version);
           
           // Spring Boot 3.5.x 应该使用 Spring Framework 6.2.x
           assertTrue("Spring Framework version should be 6.2.x for Spring Boot 3.5.x, got: " + version,
               version.startsWith("6.2.") || version.startsWith("6.3."));
       }
   }
   ```

3. 创建 JakartaEEVersionTest.java：
   ```java
   package com.github.hadoken.framework.upgrade;
   
   import org.junit.Test;
   
   import jakarta.servlet.http.HttpServlet;
   import jakarta.ws.rs.core.Application;
   
   import static org.junit.Assert.assertTrue;
   
   public class JakartaEEVersionTest {
       
       @Test
       public void testJakartaServletVersion() {
           // 检查 Jakarta Servlet API 版本
           Package servletPackage = HttpServlet.class.getPackage();
           String specificationVersion = servletPackage.getSpecificationVersion();
           
           assertTrue("Jakarta Servlet API specification should be 5.0 or higher for Spring Boot 3.5.x, got: " + specificationVersion,
               specificationVersion != null && 
               (specificationVersion.startsWith("5.") || 
                specificationVersion.startsWith("6.")));
           
           System.out.println("Jakarta Servlet API Version: " + specificationVersion);
       }
       
       @Test
       public void testJakartaRSVersion() {
           // 检查 Jakarta RESTful Web Services API 版本
           Package rsPackage = Application.class.getPackage();
           String specificationVersion = rsPackage.getSpecificationVersion();
           
           assertTrue("Jakarta RESTful Web Services API specification should be 3.1 or higher for Spring Boot 3.5.x, got: " + specificationVersion,
               specificationVersion != null && 
               (specificationVersion.startsWith("3.")));
           
           System.out.println("Jakarta RESTful Web Services API Version: " + specificationVersion);
       }
   }
   ```

4. 运行测试验证：
   ```bash
   mvn test -Dtest=SpringBootVersionTest,SpringFrameworkVersionTest,JakartaEEVersionTest
   ```
  </action>
  <verify>
    <automated>mvn test -Dtest=SpringBootVersionTest,SpringFrameworkVersionTest,JakartaEEVersionTest</automated>
  </verify>
  <done>
    三个测试文件已创建，并且测试通过，验证了：
    1. Spring Boot 版本为 3.5.x
    2. Spring Framework 版本为 6.2.x 或 6.3.x（与 Spring Boot 3.5.x 兼容）
    3. Jakarta EE API 版本为 5.0+ 和 3.1+（与 Spring Boot 3.5.x 兼容）
  </done>
</task>

<task type="auto">
  <name>任务 4: 验证项目编译和依赖解析</name>
  <files>N/A（Maven命令）</files>
  <action>
1. 运行 Maven 编译命令检查依赖兼容性：
   ```bash
   mvn clean compile -DskipTests
   ```

2. 如果编译失败：
   - 检查错误信息，确定是版本冲突还是其他问题
   - 如果是 Spring Boot 3.5.x 相关的问题，可能需要调整其他依赖版本
   - 记录任何编译错误到 NOTES.md

3. 如果编译成功：
   - 运行依赖树分析确认所有传递依赖版本正确：
   ```bash
   mvn dependency:tree -Dincludes=org.springframework.boot
   ```
   
   - 验证 Spring Boot 相关依赖版本：
   ```bash
   mvn dependency:tree | grep -E "(spring-boot|spring-core|spring-context|spring-web)" | head -20
   ```

4. 记录结果到 NOTES.md：
   - 编译状态：成功/失败
   - 遇到的任何问题
   - 建议的解决方案（如果有）
  </action>
  <verify>
    <automated>mvn clean compile -DskipTests 2>&1 | grep -E "BUILD SUCCESS|BUILD FAILURE"</automated>
  </verify>
  <done>项目能够在 Spring Boot 3.5.x 下成功编译，依赖树显示正确的 Spring Boot 和 Spring Framework 版本</done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | Description |
|----------|-------------|
| 构建系统 → Maven 仓库 | 从 Maven Central 下载依赖时可能存在依赖混淆攻击 |
| 版本声明 → 实际依赖 | pom.xml 中声明的版本可能与实际解析的版本不一致 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | 缓解方案 |
|-----------|----------|-----------|-------------|----------|
| T-03-01 | Tampering | hadoken-dependencies/pom.xml | mitigate | 使用 Maven 签名验证和 checksum 检查依赖完整性 |
| T-03-02 | Spoofing | Maven 仓库镜像 | accept | 使用可信的 Maven 仓库（华为云、阿里云镜像），风险较低 |
| T-03-03 | Repudiation | 版本变更 | mitigate | 在 NOTES.md 中记录版本变更原因和验证结果 |
| T-03-04 | Information Disclosure | 编译输出中的版本信息 | accept | 版本信息是公开的，不包含敏感数据 |
| T-03-05 | Denial of Service | 依赖解析失败 | mitigate | 提供明确的错误信息和回滚方案 |
| T-03-06 | Elevation of Privilege | 依赖注入漏洞 | mitigate | 使用 Spring Boot BOM 管理版本，确保使用安全版本 |

</threat_model>

<verification>
1. **版本验证**：确认 hadoken-dependencies/pom.xml 中的 spring-boot.version 已更新为 3.5.x
2. **编译验证**：运行 `mvn clean compile -DskipTests` 成功完成
3. **测试验证**：运行 SpringBootVersionTest、SpringFrameworkVersionTest、JakartaEEVersionTest 全部通过
4. **依赖树验证**：运行 `mvn dependency:tree -Dincludes=org.springframework.boot` 显示正确的版本

**自动化验证命令：**
```bash
# 综合验证
mvn clean test -Dtest=SpringBootVersionTest,SpringFrameworkVersionTest,JakartaEEVersionTest && \
echo "✅ Spring Boot 3.5.x 升级成功"
```
</verification>

<success_criteria>
1. ✅ hadoken-dependencies/pom.xml 中 spring-boot.version 已更新为 3.5.x（如 3.5.0）
2. ✅ 项目使用 `mvn clean compile -DskipTests` 成功编译
3. ✅ SpringBootVersionTest 通过，确认 Spring Boot 版本为 3.5.x
4. ✅ SpringFrameworkVersionTest 通过，确认 Spring Framework 版本与 Spring Boot 3.5.x 兼容
5. ✅ JakartaEEVersionTest 通过，确认 Jakarta EE API 版本与 Spring Boot 3.5.x 兼容
6. ✅ 依赖树显示正确的 Spring Boot 和相关依赖版本
</success_criteria>

<output>
完成此计划后，创建 `.planning/phases/03-core-framework-upgrade/03-01-SUMMARY.md`

总结内容应包括：
- 实际使用的 Spring Boot 版本号
- 编译结果和遇到的任何问题
- 测试执行结果
- 下一步建议（如需要调整其他依赖版本）
</output>