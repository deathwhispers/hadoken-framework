---
phase: 03-core-framework-upgrade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hadoken-dependencies/pom.xml
  - .planning/phases/03-core-framework-upgrade/03-02-SUMMARY.md
autonomous: true
requirements: [CORE-02, CORE-03]
user_setup: []

must_haves:
  truths:
    - "Spring Cloud 版本与 Spring Boot 3.5.x 兼容"
    - "Spring Cloud Alibaba 版本与 Spring Cloud 版本兼容"
    - "微服务相关功能在升级后保持正常"
  artifacts:
    - path: "hadoken-dependencies/pom.xml"
      provides: "Spring Cloud 和 Spring Cloud Alibaba 版本配置"
      contains: 
        - "<spring-cloud.version>2025.0.x</spring-cloud.version>"
        - "<spring-cloud-alibaba.version>2023.0.1.0+</spring-cloud-alibaba.version>"
    - path: "src/test/java/com/github/hadoken/framework/upgrade/SpringCloudCompatibilityTest.java"
      provides: "Spring Cloud 兼容性验证测试"
      min_lines: 30
    - path: "src/test/java/com/github/hadoken/framework/upgrade/AlibabaCompatibilityTest.java"
      provides: "Spring Cloud Alibaba 兼容性验证测试"
      min_lines: 30
  key_links:
    - from: "hadoken-dependencies/pom.xml"
      to: "Spring Cloud BOM"
      via: "dependencyManagement import"
      pattern: "spring-cloud-dependencies.*2025"
    - from: "hadoken-dependencies/pom.xml"
      to: "Spring Cloud Alibaba BOM"
      via: "dependencyManagement import"
      pattern: "spring-cloud-alibaba-dependencies.*2023"
---

<objective>
升级 Spring Cloud 到与 Spring Boot 3.5.x 兼容的版本（2025.0.x），并验证 Spring Cloud Alibaba 的兼容性，确保微服务功能在升级后正常工作。

Purpose: Spring Cloud 是微服务架构的核心，必须与 Spring Boot 版本保持兼容。Spring Cloud Alibaba 作为阿里云组件，需要与 Spring Cloud 版本兼容。
Output: 
- hadoken-dependencies/pom.xml 更新 Spring Cloud 和 Spring Cloud Alibaba 版本
- Spring Cloud 兼容性验证测试
- Spring Cloud Alibaba 兼容性验证测试
- 兼容性验证报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 用户决策
@.planning/phases/03-core-framework-upgrade/03-CONTEXT.md

# 研究结果
@.planning/phases/03-core-framework-upgrade/03-RESEARCH.md

# JDK 25 代码规范
@.planning/docs/JDK25-CODE-STANDARDS.md

# 当前版本状态
@hadoken-dependencies/pom.xml

# 现有 Spring Cloud 和 Alibaba 版本
1. 当前 Spring Cloud 版本: 2025.0.0（已与 Spring Boot 3.3.13 兼容）
2. 当前 Spring Cloud Alibaba 版本: 2023.0.1.0
3. 根据用户决策 D-04: Spring Cloud 2025.0.x 与 Spring Boot 3.5.x 兼容
4. 根据用户决策 D-07: Spring Cloud Alibaba 2023.0.1.0+ 与 Spring Cloud 2025.x 兼容
5. 根据研究假设 A2: Spring Cloud 2025.0.x 与 Spring Boot 3.5.x 兼容 [需要验证]
6. 根据研究假设 A3: Spring Cloud Alibaba 2023.0.1.0+ 与 Spring Cloud 2025.x 兼容 [需要验证]
</context>

<tasks>

<task type="auto">
  <name>任务 1: 验证 Spring Cloud 与 Spring Boot 3.5.x 的兼容性</name>
  <files>N/A（检查外部资源）</files>
  <action>
1. 检查 Spring Cloud 官方兼容性矩阵，确认与 Spring Boot 3.5.x 兼容的版本：
   - Spring Boot 3.5.x 应该与 Spring Cloud 2025.0.x 兼容
   - 确认具体的兼容版本号（如 2025.0.0, 2025.0.1, 2025.0.2）

2. 如果无法通过网络验证，采用保守策略：
   - 保持当前 Spring Cloud 版本 2025.0.0（假设与 Spring Boot 3.5.x 兼容）
   - 在任务 3 的测试中验证兼容性

3. 确定版本号后，记录在 NOTES.md 中：
   - Spring Cloud 版本：如 2025.0.0
   - 与 Spring Boot 3.5.x 的兼容性状态
   - 任何已知的兼容性问题或注意事项
  </action>
  <verify>
    Spring Cloud 版本信息和兼容性记录在 .planning/phases/03-core-framework-upgrade/NOTES.md 中
  </verify>
  <done>确定了与 Spring Boot 3.5.x 兼容的 Spring Cloud 版本</done>
</task>

<task type="auto">
  <name>任务 2: 验证 Spring Cloud Alibaba 与 Spring Cloud 2025.x 的兼容性</name>
  <files>N/A（检查外部资源）</files>
  <action>
1. 检查 Spring Cloud Alibaba 官方文档，确认与 Spring Cloud 2025.x 兼容的版本：
   - Spring Cloud 2025.x 应该与 Spring Cloud Alibaba 2023.0.1.0+ 兼容
   - 确认具体的兼容版本号（如 2023.0.1.0, 2023.0.2.0）

2. 如果无法找到官方兼容性信息，采用测试验证策略：
   - 保持当前 Spring Cloud Alibaba 版本 2023.0.1.0
   - 在任务 4 的测试中验证兼容性
   - 如果测试失败，根据错误信息调整版本

3. 确定版本号后，记录在 NOTES.md 中：
   - Spring Cloud Alibaba 版本：如 2023.0.1.0
   - 与 Spring Cloud 2025.x 的兼容性状态
   - 备选方案：如果无兼容版本，考虑降级 Spring Cloud 或移除 Alibaba 依赖
  </action>
  <verify>
    Spring Cloud Alibaba 版本信息和兼容性记录在 .planning/phases/03-core-framework-upgrade/NOTES.md 中
  </verify>
  <done>确定了与 Spring Cloud 2025.x 兼容的 Spring Cloud Alibaba 版本（或备选方案）</done>
</task>

<task type="auto">
  <name>任务 3: 更新 hadoken-dependencies 中的 Spring Cloud 和 Alibaba 版本</name>
  <files>hadoken-dependencies/pom.xml</files>
  <action>
1. 打开 hadoken-dependencies/pom.xml

2. 在 properties 部分找到 spring-cloud.version 属性（当前值为 2025.0.0）

3. 根据任务 1 的结果，如果需要更新版本，则修改为：
   ```xml
   <spring-cloud.version>2025.0.0</spring-cloud.version>  <!-- 或更新版本 -->
   ```

4. 在 properties 部分找到 spring-cloud-alibaba.version 属性（当前值为 2023.0.1.0）

5. 根据任务 2 的结果，如果需要更新版本，则修改为：
   ```xml
   <spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>  <!-- 或更新版本 -->
   ```

6. 如果 Spring Cloud Alibaba 无兼容版本，添加注释说明备选方案：
   ```xml
   <!-- 
   Spring Cloud Alibaba 2023.0.1.0 可能与 Spring Cloud 2025.x 不兼容
   如果测试失败，备选方案：
   1. 降级 Spring Cloud 到 2023.x
   2. 移除 Spring Cloud Alibaba 依赖
   -->
   ```

7. 保存更改
  </action>
  <verify>
    <automated>grep -n "spring-cloud.version" hadoken-dependencies/pom.xml | grep "2025"</automated>
    <automated>grep -n "spring-cloud-alibaba.version" hadoken-dependencies/pom.xml | grep "2023"</automated>
  </verify>
  <done>hadoken-dependencies/pom.xml 中的 Spring Cloud 和 Spring Cloud Alibaba 版本已根据兼容性验证更新</done>
</task>

<task type="auto" tdd="true">
  <name>任务 4: 创建 Spring Cloud 和 Alibaba 兼容性测试</name>
  <files>
    src/test/java/com/github/hadoken/framework/upgrade/SpringCloudCompatibilityTest.java
    src/test/java/com/github/hadoken/framework/upgrade/AlibabaCompatibilityTest.java
  </files>
  <behavior>
    - 测试 1: SpringCloudCompatibilityTest 验证 Spring Cloud 核心功能可用
    - 测试 2: AlibabaCompatibilityTest 验证 Spring Cloud Alibaba 组件可用
    - 测试 3: 验证微服务相关功能的基本兼容性
  </behavior>
  <action>
1. 创建 SpringCloudCompatibilityTest.java：
   ```java
   package com.github.hadoken.framework.upgrade;
   
   import org.junit.Test;
   import org.springframework.cloud.client.ServiceInstance;
   import org.springframework.cloud.client.loadbalancer.LoadBalancerClient;
   import org.springframework.cloud.config.client.ConfigClientProperties;
   import org.springframework.cloud.context.config.annotation.RefreshScope;
   
   import java.lang.annotation.Annotation;
   
   import static org.junit.Assert.assertNotNull;
   import static org.junit.Assert.assertTrue;
   
   public class SpringCloudCompatibilityTest {
       
       @Test
       public void testSpringCloudCoreClassesAvailable() {
           // 验证 Spring Cloud 核心类可用
           Class<?> loadBalancerClientClass = LoadBalancerClient.class;
           Class<?> serviceInstanceClass = ServiceInstance.class;
           Class<?> configClientPropertiesClass = ConfigClientProperties.class;
           
           assertNotNull("LoadBalancerClient class should be available", loadBalancerClientClass);
           assertNotNull("ServiceInstance class should be available", serviceInstanceClass);
           assertNotNull("ConfigClientProperties class should be available", configClientPropertiesClass);
           
           System.out.println("Spring Cloud core classes are available");
       }
       
       @Test
       public void testSpringCloudAnnotationsAvailable() {
           // 验证 Spring Cloud 注解可用
           Class<? extends Annotation> refreshScopeClass = RefreshScope.class;
           
           assertNotNull("@RefreshScope annotation should be available", refreshScopeClass);
           
           // 检查注解的保留策略
           Annotation retention = refreshScopeClass.getAnnotation(java.lang.annotation.Retention.class);
           assertNotNull("@RefreshScope should have @Retention annotation", retention);
           
           System.out.println("Spring Cloud annotations are available");
       }
       
       @Test
       public void testSpringCloudVersion() {
           // 获取 Spring Cloud 版本信息
           Package cloudPackage = LoadBalancerClient.class.getPackage();
           String implementationVersion = cloudPackage.getImplementationVersion();
           String specificationVersion = cloudPackage.getSpecificationVersion();
           
           System.out.println("Spring Cloud Implementation Version: " + implementationVersion);
           System.out.println("Spring Cloud Specification Version: " + specificationVersion);
           
           // 验证版本为 2025.x
           if (implementationVersion != null) {
               assertTrue("Spring Cloud version should be 2025.x, got: " + implementationVersion,
                   implementationVersion.startsWith("2025"));
           }
       }
   }
   ```

2. 创建 AlibabaCompatibilityTest.java：
   ```java
   package com.github.hadoken.framework.upgrade;
   
   import org.junit.Test;
   
   import com.alibaba.cloud.nacos.NacosDiscoveryProperties;
   import com.alibaba.cloud.nacos.NacosServiceManager;
   import com.alibaba.cloud.sentinel.SentinelProperties;
   
   import static org.junit.Assert.assertNotNull;
   import static org.junit.Assert.assertTrue;
   
   public class AlibabaCompatibilityTest {
       
       @Test
       public void testAlibabaCoreClassesAvailable() {
           // 验证 Spring Cloud Alibaba 核心类可用
           try {
               Class<?> nacosDiscoveryPropertiesClass = NacosDiscoveryProperties.class;
               Class<?> nacosServiceManagerClass = NacosServiceManager.class;
               Class<?> sentinelPropertiesClass = SentinelProperties.class;
               
               assertNotNull("NacosDiscoveryProperties class should be available", nacosDiscoveryPropertiesClass);
               assertNotNull("NacosServiceManager class should be available", nacosServiceManagerClass);
               assertNotNull("SentinelProperties class should be available", sentinelPropertiesClass);
               
               System.out.println("Spring Cloud Alibaba core classes are available");
           } catch (NoClassDefFoundError e) {
               System.out.println("WARNING: Spring Cloud Alibaba classes not found. " +
                                 "This may indicate compatibility issues with Spring Cloud 2025.x.");
               System.out.println("Error: " + e.getMessage());
               
               // 如果类不存在，记录到 NOTES.md
               // 这可能是兼容性问题的迹象
           }
       }
       
       @Test
       public void testAlibabaVersion() {
           // 获取 Spring Cloud Alibaba 版本信息
           try {
               Package alibabaPackage = NacosDiscoveryProperties.class.getPackage();
               String implementationVersion = alibabaPackage.getImplementationVersion();
               String specificationVersion = alibabaPackage.getSpecificationVersion();
               
               System.out.println("Spring Cloud Alibaba Implementation Version: " + implementationVersion);
               System.out.println("Spring Cloud Alibaba Specification Version: " + specificationVersion);
               
               // 验证版本为 2023.x
               if (implementationVersion != null) {
                   assertTrue("Spring Cloud Alibaba version should be 2023.x, got: " + implementationVersion,
                       implementationVersion.startsWith("2023"));
               }
           } catch (NoClassDefFoundError e) {
               System.out.println("Cannot check Alibaba version: " + e.getMessage());
           }
       }
       
       @Test
       public void testAlibabaCompatibilityWithSpringCloud() {
           // 验证 Alibaba 与 Spring Cloud 的兼容性
           try {
               // 尝试创建 Alibaba 配置对象
               Class<?> nacosConfigClass = Class.forName("com.alibaba.cloud.nacos.NacosConfigProperties");
               Class<?> sentinelDataSourceClass = Class.forName("com.alibaba.cloud.sentinel.datasource.config.DataSourcePropertiesConfiguration");
               
               assertNotNull("NacosConfigProperties class should be available", nacosConfigClass);
               assertNotNull("DataSourcePropertiesConfiguration class should be available", sentinelDataSourceClass);
               
               System.out.println("Spring Cloud Alibaba configuration classes are available");
           } catch (ClassNotFoundException | NoClassDefFoundError e) {
               System.out.println("WARNING: Some Spring Cloud Alibaba configuration classes not found.");
               System.out.println("This may indicate compatibility issues or missing dependencies.");
               System.out.println("Error: " + e.getMessage());
           }
       }
   }
   ```

3. 运行测试验证：
   ```bash
   mvn test -Dtest=SpringCloudCompatibilityTest,AlibabaCompatibilityTest
   ```
  </action>
  <verify>
    <automated>mvn test -Dtest=SpringCloudCompatibilityTest,AlibabaCompatibilityTest</automated>
  </verify>
  <done>
    两个测试文件已创建，并且测试通过或提供了明确的兼容性状态报告。
    测试验证了：
    1. Spring Cloud 核心功能可用，版本为 2025.x
    2. Spring Cloud Alibaba 组件可用性（可能发现兼容性问题）
    3. 微服务相关功能的基本兼容性
  </done>
</task>

<task type="auto">
  <name>任务 5: 验证依赖解析和编译</name>
  <files>N/A（Maven命令）</files>
  <action>
1. 运行 Maven 编译命令检查 Spring Cloud 和 Alibaba 依赖兼容性：
   ```bash
   mvn clean compile -DskipTests
   ```

2. 如果编译失败，分析错误：
   - 检查是否是 Spring Cloud 与 Spring Boot 版本不兼容
   - 检查是否是 Spring Cloud Alibaba 与 Spring Cloud 版本不兼容
   - 根据错误信息调整版本或采取备选方案

3. 运行依赖树分析：
   ```bash
   mvn dependency:tree -Dincludes=org.springframework.cloud,com.alibaba.cloud
   ```

4. 检查关键依赖版本：
   ```bash
   mvn dependency:tree | grep -E "(spring-cloud|alibaba)" | head -30
   ```

5. 记录结果到 NOTES.md：
   - 编译状态
   - 发现的任何兼容性问题
   - 采取的解决方案
   - Spring Cloud Alibaba 的兼容性状态
  </action>
  <verify>
    <automated>mvn clean compile -DskipTests 2>&1 | grep -E "BUILD SUCCESS|BUILD FAILURE"</automated>
  </verify>
  <done>
    项目能够在更新后的 Spring Cloud 和 Spring Cloud Alibaba 版本下成功编译，
    或者明确了兼容性问题的具体原因和解决方案。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | Description |
|----------|-------------|
| Spring Cloud BOM → 实际依赖 | BOM 声明的版本可能与实际解析的版本不一致 |
| Spring Cloud Alibaba → Spring Cloud | 第三方组件与核心框架的兼容性风险 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | 缓解方案 |
|-----------|----------|-----------|-------------|----------|
| T-03-07 | Tampering | Spring Cloud 依赖 | mitigate | 使用 Spring 官方 BOM 和签名验证 |
| T-03-08 | Spoofing | 阿里云组件源 | accept | 使用官方 Maven 仓库，风险可控 |
| T-03-09 | Repudiation | 版本兼容性声明 | mitigate | 通过自动化测试验证兼容性 |
| T-03-10 | Information Disclosure | 微服务配置 | accept | 测试中不包含生产配置 |
| T-03-11 | Denial of Service | 版本冲突导致启动失败 | mitigate | 提供明确的错误信息和回滚方案 |
| T-03-12 | Elevation of Privilege | 微服务安全漏洞 | mitigate | 使用 Spring Security 和 Spring Cloud Security |

</threat_model>

<verification>
1. **版本验证**：确认 hadoken-dependencies/pom.xml 中的 spring-cloud.version 和 spring-cloud-alibaba.version 已正确设置
2. **编译验证**：运行 `mvn clean compile -DskipTests` 成功完成
3. **测试验证**：运行 SpringCloudCompatibilityTest 和 AlibabaCompatibilityTest 测试
4. **依赖树验证**：运行 `mvn dependency:tree -Dincludes=org.springframework.cloud,com.alibaba.cloud` 显示正确的版本

**自动化验证命令：**
```bash
# 综合验证
mvn clean test -Dtest=SpringCloudCompatibilityTest,AlibabaCompatibilityTest && \
echo "✅ Spring Cloud 和 Alibaba 升级验证成功"
```

**兼容性状态报告：**
测试完成后，生成兼容性状态报告，包括：
- Spring Cloud 2025.x 与 Spring Boot 3.5.x 的兼容性状态
- Spring Cloud Alibaba 与 Spring Cloud 2025.x 的兼容性状态
- 发现的任何问题和解决方案
</verification>

<success_criteria>
1. ✅ hadoken-dependencies/pom.xml 中 Spring Cloud 版本为 2025.0.x
2. ✅ hadoken-dependencies/pom.xml 中 Spring Cloud Alibaba 版本为 2023.0.1.0+（或明确的备选方案）
3. ✅ 项目使用 `mvn clean compile -DskipTests` 成功编译
4. ✅ SpringCloudCompatibilityTest 通过，确认 Spring Cloud 核心功能可用
5. ✅ AlibabaCompatibilityTest 提供明确的兼容性状态报告（通过或识别问题）
6. ✅ 依赖树显示正确的 Spring Cloud 和 Alibaba 依赖版本
7. ✅ 兼容性状态和任何问题记录在 NOTES.md 中
</success_criteria>

<output>
完成此计划后，创建 `.planning/phases/03-core-framework-upgrade/03-02-SUMMARY.md`

总结内容应包括：
- 实际使用的 Spring Cloud 和 Spring Cloud Alibaba 版本号
- 兼容性验证结果
- 编译结果和遇到的任何问题
- Spring Cloud Alibaba 的兼容性状态（兼容/不兼容/需要调整）
- 下一步建议（如需要调整版本或采取备选方案）
</output>