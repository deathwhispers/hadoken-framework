---
phase: 01-build-environment-code-standards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: 
  - .planning/docs/JDK25-CODE-STANDARDS.md
autonomous: true
requirements: [STD-01, STD-02, STD-03, STD-04]
user_setup: []

must_haves:
  truths:
    - "JDK 25 新特性代码规范文档已创建并可被开发团队访问"
    - "规范文档涵盖 Virtual Threads、Pattern Matching、Scoped Values、Value Types 四大主题"
    - "每个主题包含概念说明、使用场景、代码示例、注意事项"
    - "规范强调稳定性优先，不启用 Preview Features，仅使用 JDK 25 正式特性"
  artifacts:
    - path: ".planning/docs/JDK25-CODE-STANDARDS.md"
      provides: "JDK 25 代码规范文档"
      min_lines: 150
      contains: "# JDK 25 代码规范"
    - path: ".planning/docs/JDK25-CODE-STANDARDS.md"
      provides: "Virtual Threads 使用指南"
      contains: "## Virtual Threads"
    - path: ".planning/docs/JDK25-CODE-STANDARDS.md"
      provides: "Pattern Matching 使用指南"
      contains: "## Pattern Matching"
    - path: ".planning/docs/JDK25-CODE-STANDARDS.md"
      provides: "Scoped Values 使用指南"
      contains: "## Scoped Values"
    - path: ".planning/docs/JDK25-CODE-STANDARDS.md"
      provides: "Value Types 前瞻性说明"
      contains: "## Value Types"
  key_links:
    - from: ".planning/docs/JDK25-CODE-STANDARDS.md"
      to: "CONTEXT.md 决策"
      via: "实现 D-01 到 D-10 的所有决策"
      pattern: "每个决策在文档中都有对应实现"
    - from: ".planning/docs/JDK25-CODE-STANDARDS.md"
      to: "项目开发工作流"
      via: "为后续阶段提供编码规范"
      pattern: "文档结构清晰，便于开发团队参考"
---

<objective>
创建 JDK 25 新特性代码规范文档，为开发团队提供明确的使用指南。

Purpose: 确保开发团队在后续阶段使用 JDK 25 新特性时有明确的规范可遵循，避免不一致的编码风格和潜在问题。
Output: `.planning/docs/JDK25-CODE-STANDARDS.md` 文档，包含四大主题的详细规范。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 用户决策 (来自 CONTEXT.md)
- D-01: 规范文档包含四大主题：Virtual Threads、Pattern Matching、Scoped Values、Value Types
- D-02: 每个主题包含：概念说明、使用场景、代码示例、注意事项
- D-03: 规范强调稳定性优先，不启用 Preview Features，仅使用 JDK 25 正式特性
- D-04: Virtual Threads 使用指南：适用于 I/O 密集型任务，不适用于 CPU 密集型任务
- D-05: Pattern Matching 使用指南：优先用于记录类解构和 switch 表达式
- D-06: Scoped Values 使用指南：替代 ThreadLocal 和 InheritableThreadLocal
- D-07: Value Types 使用指南：等待正式发布后引入，当前仅作为前瞻性说明
- D-08: 规范文档存放位置：`.planning/docs/JDK25-CODE-STANDARDS.md`
- D-09: 文档格式：Markdown，便于阅读和版本控制
- D-10: 文档结构：概述 → 各主题章节 → 最佳实践 → 参考资源

# 需求映射
- STD-01: 制定 JDK 25 新特性代码规范文档
- STD-02: 记录类模式匹配使用规范
- STD-03: 虚拟线程使用规范
- STD-04: 值类型使用规范（如适用）

# 研究参考 (来自 RESEARCH.md)
- JDK 25 新特性说明：Virtual Threads、Pattern Matching、Scoped Values、Value Types
- 代码示例和最佳实践
- 常见陷阱和避免方法
</context>

<tasks>

<task type="auto">
  <name>任务 1: 创建规范文档目录和基础结构</name>
  <files>.planning/docs/JDK25-CODE-STANDARDS.md</files>
  <action>
创建 JDK 25 代码规范文档，按照 D-08、D-09、D-10 决策设置文档位置、格式和结构：

1. 创建目录：`.planning/docs/`（如果不存在）
2. 创建文件：`JDK25-CODE-STANDARDS.md`
3. 按照 D-10 要求设置文档结构：
   - 文档标题和概述
   - 目录（TOC）
   - 四大主题章节（Virtual Threads、Pattern Matching、Scoped Values、Value Types）
   - 最佳实践章节
   - 参考资源章节

文档必须使用 Markdown 格式（per D-09），位置在 `.planning/docs/`（per D-08）。

在概述部分明确说明：本规范基于 JDK 25 正式特性，不启用 Preview Features（per D-03）。
  </action>
  <verify>
<automated>test -f .planning/docs/JDK25-CODE-STANDARDS.md && echo "文件存在" || echo "文件不存在"</automated>
<automated>grep -c "^# JDK 25 代码规范$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "^## 目录$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "不启用 Preview Features" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
  </verify>
  <done>JDK 25 代码规范文档已创建，包含概述、目录和稳定性声明。</done>
</task>

<task type="auto">
  <name>任务 2: 编写 Virtual Threads 规范章节</name>
  <files>.planning/docs/JDK25-CODE-STANDARDS.md</files>
  <action>
编写 Virtual Threads 规范章节，实现 D-01、D-02、D-04 决策：

1. 创建 "## Virtual Threads" 章节
2. 按照 D-02 结构编写：
   - **概念说明**：解释虚拟线程是什么，与传统平台线程的区别
   - **使用场景**：根据 D-04，明确适用于 I/O 密集型任务，不适用于 CPU 密集型任务
   - **代码示例**：提供实际的 Java 代码示例，展示如何创建和使用虚拟线程
   - **注意事项**：列出使用虚拟线程时的注意事项和最佳实践

代码示例应包括：
- 使用 `Executors.newVirtualThreadPerTaskExecutor()` 创建虚拟线程执行器
- I/O 密集型任务的示例（如 HTTP 请求、数据库查询）
- 明确不适用于 CPU 密集型任务的说明和替代方案

强调虚拟线程的优势：减少内存占用、提高并发性能，但需要正确使用场景。
  </action>
  <verify>
<automated>grep -c "^## Virtual Threads$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "### 概念说明" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "### 使用场景" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "I/O 密集型任务" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "CPU 密集型任务" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "Executors.newVirtualThreadPerTaskExecutor" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
  </verify>
  <done>Virtual Threads 章节已完整编写，包含概念、场景、示例和注意事项，符合 D-04 指导原则。</done>
</task>

<task type="auto">
  <name>任务 3: 编写 Pattern Matching 和 Scoped Values 规范章节</name>
  <files>.planning/docs/JDK25-CODE-STANDARDS.md</files>
  <action>
编写 Pattern Matching 和 Scoped Values 规范章节，实现 D-01、D-02、D-05、D-06 决策：

**Pattern Matching 章节 (D-05):**
1. 创建 "## Pattern Matching" 章节
2. 按照 D-02 结构编写：
   - **概念说明**：解释模式匹配是什么，在 Java 中的演进
   - **使用场景**：根据 D-05，优先用于记录类解构和 switch 表达式
   - **代码示例**：提供记录类解构、switch 表达式中的模式匹配示例
   - **注意事项**：类型安全、可读性、性能考虑

**Scoped Values 章节 (D-06):**
1. 创建 "## Scoped Values" 章节
2. 按照 D-02 结构编写：
   - **概念说明**：解释 Scoped Values 是什么，与 ThreadLocal 的区别
   - **使用场景**：根据 D-06，替代 ThreadLocal 和 InheritableThreadLocal
   - **代码示例**：展示如何使用 ScopedValue 进行值传递
   - **注意事项**：生命周期管理、性能影响

为每个章节提供实际的 Spring Boot 应用场景示例，展示如何在实际项目中使用这些特性。
  </action>
  <verify>
<automated>grep -c "^## Pattern Matching$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "记录类解构" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "switch 表达式" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "^## Scoped Values$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "ThreadLocal" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "InheritableThreadLocal" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
  </verify>
  <done>Pattern Matching 和 Scoped Values 章节已完整编写，包含概念、场景、示例和注意事项。</done>
</task>

<task type="auto">
  <name>任务 4: 编写 Value Types 规范章节和总结</name>
  <files>.planning/docs/JDK25-CODE-STANDARDS.md</files>
  <action>
编写 Value Types 规范章节和文档总结，实现 D-01、D-02、D-07、D-10 决策：

**Value Types 章节 (D-07):**
1. 创建 "## Value Types" 章节
2. 按照 D-02 结构编写：
   - **概念说明**：解释值类型是什么，与引用类型的区别
   - **使用场景**：根据 D-07，说明当前仅作为前瞻性说明，等待正式发布后引入
   - **代码示例**：提供简单的值类型定义示例（基于当前提案）
   - **注意事项**：强调这是预览特性，当前不建议在生产中使用

**文档总结部分：**
1. 创建 "## 最佳实践" 章节：总结四大特性的最佳使用方式
2. 创建 "## 参考资源" 章节：提供官方文档链接和进一步学习资源
3. 确保文档整体结构符合 D-10：概述 → 各主题章节 → 最佳实践 → 参考资源

文档应提供实用指导，帮助开发团队在实际项目中正确应用 JDK 25 新特性。
  </action>
  <verify>
<automated>grep -c "^## Value Types$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "前瞻性说明" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "等待正式发布后引入" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "^## 最佳实践$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>grep -c "^## 参考资源$" .planning/docs/JDK25-CODE-STANDARDS.md</automated>
<automated>wc -l .planning/docs/JDK25-CODE-STANDARDS.md | awk '{print $1}'</automated>
  </verify>
  <done>Value Types 章节和文档总结已完整编写，文档结构完整，符合所有决策要求。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 规范文档 → 开发实践 | 文档内容指导实际编码，错误的指导可能导致代码质量问题 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|----------|
| T-01-04 | Spoofing | 规范文档权威性 | mitigate | 明确标注规范基于 JDK 25 官方文档和 Spring Boot 最佳实践 |
| T-01-05 | Repudiation | 规范变更记录 | accept | 使用 Git 版本控制跟踪文档变更，接受此风险 |
| T-01-06 | Information Disclosure | 技术细节暴露 | accept | 文档仅包含公开的技术信息，无敏感内容，接受此风险 |
</threat_model>

<verification>
## 整体验证

1. **文档完整性验证**：确保所有四大主题章节都存在且结构完整
2. **决策遵从性验证**：验证所有用户决策（D-01 到 D-10）已在文档中实现
3. **内容质量验证**：检查文档是否提供实用的代码示例和最佳实践

验证命令：
```bash
# 1. 检查文档结构和完整性
grep -n "^#" .planning/docs/JDK25-CODE-STANDARDS.md

# 2. 检查决策实现
echo "检查 D-01（四大主题）:"
grep -c "Virtual Threads\|Pattern Matching\|Scoped Values\|Value Types" .planning/docs/JDK25-CODE-STANDARDS.md

echo "检查 D-02（章节结构）:"
grep -c "概念说明\|使用场景\|代码示例\|注意事项" .planning/docs/JDK25-CODE-STANDARDS.md

echo "检查 D-03（稳定性优先）:"
grep -c "不启用 Preview Features\|正式特性" .planning/docs/JDK25-CODE-STANDARDS.md

# 3. 检查文档长度和质量
wc -l .planning/docs/JDK25-CODE-STANDARDS.md
```
</verification>

<success_criteria>
## 成功标准

1. ✅ `.planning/docs/JDK25-CODE-STANDARDS.md` 文件已创建
2. ✅ 文档包含四大主题章节：Virtual Threads、Pattern Matching、Scoped Values、Value Types
3. ✅ 每个主题章节包含概念说明、使用场景、代码示例、注意事项四个部分
4. ✅ 文档明确声明不启用 Preview Features，仅使用 JDK 25 正式特性
5. ✅ Virtual Threads 章节明确说明适用于 I/O 密集型任务，不适用于 CPU 密集型任务
6. ✅ Pattern Matching 章节优先推荐用于记录类解构和 switch 表达式
7. ✅ Scoped Values 章节说明替代 ThreadLocal 和 InheritableThreadLocal
8. ✅ Value Types 章节作为前瞻性说明，等待正式发布后引入
9. ✅ 文档结构符合要求：概述 → 各主题章节 → 最佳实践 → 参考资源
10. ✅ STD-01 到 STD-04 需求已实现
</success_criteria>

<output>
完成本计划后，创建 `.planning/phases/01-build-environment-code-standards/01-02-SUMMARY.md` 总结文件。
</output>