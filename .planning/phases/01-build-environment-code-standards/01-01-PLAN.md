---
phase: 01-build-environment-code-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - pom.xml
  - hadoken-dependencies/pom.xml
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04]
user_setup: []

must_haves:
  truths:
    - "项目可以在 JDK 25 环境下成功编译"
    - "Maven Compiler Plugin 版本为 3.14.0"
    - "Java version 属性已更新为 25"
    - "Maven Resource Plugin 版本保持为 3.3.1"
  artifacts:
    - path: "pom.xml"
      provides: "根 POM 配置，定义 java.version 和插件版本"
      contains: "<java.version>25</java.version>"
    - path: "hadoken-dependencies/pom.xml"
      provides: "依赖 BOM 配置，统一管理版本"
      contains: "<java.version>25</java.version>"
    - path: "pom.xml"
      provides: "Maven Compiler Plugin 3.14.0 配置"
      contains: "<maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>"
  key_links:
    - from: "pom.xml"
      to: "hadoken-dependencies/pom.xml"
      via: "依赖继承"
      pattern: "java.version 属性值一致"
    - from: "pom.xml"
      to: "所有子模块"
      via: "Maven 继承机制"
      pattern: "子模块继承根 POM 的版本配置"
---

<objective>
将 Hadoken Framework 的构建环境升级到支持 JDK 25，更新 Maven 和 Maven 插件版本。

Purpose: 确保项目可以在 JDK 25 环境下成功编译，为后续阶段的功能升级奠定基础。
Output: 更新后的 pom.xml 和 hadoken-dependencies/pom.xml 文件，项目支持 JDK 25 编译。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 当前 POM 配置状态
@pom.xml (根 POM 当前状态)
- `<java.version>` 当前为 17
- `<maven-compiler-plugin.version>` 当前为 3.12.1
- `<maven-resources-plugin.version>` 当前为 3.3.1

@hadoken-dependencies/pom.xml (BOM 当前状态)
- `<java.version>` 当前为 17
- `<maven-compiler-plugin.version>` 当前为 3.12.1

# 用户决策 (来自 CONTEXT.md)
- D-11: Maven Compiler Plugin 版本：3.14.0（支持 JDK 25）
- D-12: Maven Resource Plugin 版本：3.3.1（保持，兼容）
- D-13: Maven Source Plugin 版本：3.3.0（保持，兼容）
- D-14: Java version 属性：25
- D-15: Maven 要求版本：3.9.x（开发者需升级本地 Maven）
- D-16: 不启用 JDK 25 Preview Features，仅使用正式稳定特性
- D-17: 如需使用预览特性，需在后续阶段单独讨论并添加 `--enable-preview` 参数

# 需求映射
- BUILD-01: Maven 升级至 3.9.x 以支持 JDK 25
- BUILD-02: Maven Compiler Plugin 升级至 3.14.x
- BUILD-03: JDK 版本更新为 25
- BUILD-04: Maven Resource Plugin 升级至兼容版本
</context>

<tasks>

<task type="auto">
  <name>任务 1: 更新根 POM 的 Java 版本和 Maven 插件版本</name>
  <files>pom.xml</files>
  <action>
更新根 pom.xml 文件中的版本配置：

1. 将 `<java.version>` 从 17 更新为 25（per D-14）
2. 将 `<maven-compiler-plugin.version>` 从 3.12.1 更新为 3.14.0（per D-11）
3. 保持 `<maven-resources-plugin.version>` 为 3.3.1（per D-12）
4. 保持 `<maven-source-plugin.version>` 为 3.3.0（per D-13）
5. 确保 Maven Compiler Plugin 配置不使用 `--enable-preview` 参数（per D-16）

具体修改位置：
- 第 16 行：`<java.version>17</java.version>` → `<java.version>25</java.version>`
- 第 22 行：`<maven-compiler-plugin.version>3.12.1</maven-compiler-plugin.version>` → `<maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>`

注意：不要修改其他插件版本，保持现有兼容性。Maven 版本要求（3.9.x）通过文档说明，不在 POM 中配置。
  </action>
  <verify>
<automated>grep -c "&lt;java.version&gt;25&lt;/java.version&gt;" pom.xml</automated>
<automated>grep -c "&lt;maven-compiler-plugin.version&gt;3.14.0&lt;/maven-compiler-plugin.version&gt;" pom.xml</automated>
<automated>grep -c "&lt;maven-resources-plugin.version&gt;3.3.1&lt;/maven-resources-plugin.version&gt;" pom.xml</automated>
<automated>grep -c "&lt;maven-source-plugin.version&gt;3.3.0&lt;/maven-source-plugin.version&gt;" pom.xml</automated>
  </verify>
  <done>根 POM 的 Java 版本已更新为 25，Maven Compiler Plugin 版本已更新为 3.14.0，其他插件版本保持不变。</done>
</task>

<task type="auto">
  <name>任务 2: 更新 BOM POM 的 Java 版本和编译器插件版本</name>
  <files>hadoken-dependencies/pom.xml</files>
  <action>
更新 hadoken-dependencies/pom.xml 文件中的版本配置：

1. 将第 19 行的 `<java.version>` 从 17 更新为 25（per D-14）
2. 将第 25 行的 `<maven-compiler-plugin.version>` 从 3.12.1 更新为 3.14.0（per D-11）
3. 确保 BOM 中的版本与根 POM 保持一致，避免版本冲突

注意：BOM 文件是依赖版本管理的中心，所有子模块通过继承或导入 BOM 获取版本配置。保持 BOM 与根 POM 的版本一致是确保整个项目构建一致性的关键。
  </action>
  <verify>
<automated>grep -c "&lt;java.version&gt;25&lt;/java.version&gt;" hadoken-dependencies/pom.xml</automated>
<automated>grep -c "&lt;maven-compiler-plugin.version&gt;3.14.0&lt;/maven-compiler-plugin.version&gt;" hadoken-dependencies/pom.xml</automated>
<automated>diff -u <(grep "java.version\|maven-compiler-plugin.version" pom.xml | head -2) <(grep "java.version\|maven-compiler-plugin.version" hadoken-dependencies/pom.xml | head -2) | wc -l</automated>
  </verify>
  <done>BOM POM 的 Java 版本和编译器插件版本已更新，与根 POM 保持一致。</done>
</task>

<task type="auto">
  <name>任务 3: 验证项目编译配置</name>
  <files>pom.xml, hadoken-dependencies/pom.xml</files>
  <action>
验证更新后的配置能够正确工作：

1. 运行 Maven 命令检查有效 POM：`mvn help:effective-pom -Doutput=effective-pom.xml`
2. 从有效 POM 中提取关键信息验证：
   - Java 版本是否为 25
   - Maven Compiler Plugin 版本是否为 3.14.0
   - 确认没有启用预览特性参数（确保 D-16 被遵守）
3. 运行快速编译测试：`mvn clean compile -DskipTests`
4. 检查编译输出中是否有关于 JDK 25 的警告或错误

注意：由于当前环境可能没有 JDK 25，编译可能会失败，但配置验证应该通过。主要验证配置是否正确更新。
  </action>
  <verify>
<automated>mvn help:effective-pom -Doutput=effective-pom.xml 2>&1 | tail -20</automated>
<automated>grep -c "&lt;java.version&gt;25&lt;/java.version&gt;" effective-pom.xml 2>/dev/null || echo "0"</automated>
<automated>grep -c "3.14.0" effective-pom.xml 2>/dev/null | head -1</automated>
<automated>grep -c "enable-preview" effective-pom.xml 2>/dev/null || echo "0"</automated>
  </verify>
  <done>有效 POM 显示 Java 版本为 25，Maven Compiler Plugin 版本为 3.14.0，未启用预览特性，配置验证通过。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 构建配置 → 构建执行 | POM 配置控制编译过程，错误的版本配置可能导致构建失败或安全漏洞 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|---------|------|------|------|----------|
| T-01-01 | Tampering | pom.xml 配置 | mitigate | 使用 Git 版本控制跟踪配置变更，确保配置来源可信 |
| T-01-02 | Spoofing | Maven 仓库源 | accept | 使用华为云和阿里云镜像源，风险较低，接受此风险 |
| T-01-03 | Information Disclosure | 构建日志 | accept | 构建日志不包含敏感信息，接受此风险 |
</threat_model>

<verification>
## 整体验证

1. **配置一致性验证**：确保根 POM 和 BOM POM 的版本配置一致
2. **编译配置验证**：验证 Maven 有效 POM 中的配置正确性
3. **决策遵从性验证**：验证所有用户决策（D-11 到 D-17）已正确实施

验证命令：
```bash
# 1. 检查版本配置
grep "java.version\|maven-compiler-plugin.version" pom.xml hadoken-dependencies/pom.xml

# 2. 生成有效 POM 并验证
mvn help:effective-pom -Doutput=effective-pom.xml
grep -A2 -B2 "25\|3.14.0" effective-pom.xml | head -20

# 3. 清理临时文件
rm -f effective-pom.xml
```
</verification>

<success_criteria>
## 成功标准

1. ✅ 根 pom.xml 中 `<java.version>` 为 25
2. ✅ 根 pom.xml 中 `<maven-compiler-plugin.version>` 为 3.14.0
3. ✅ hadoken-dependencies/pom.xml 中 `<java.version>` 为 25
4. ✅ hadoken-dependencies/pom.xml 中 `<maven-compiler-plugin.version>` 为 3.14.0
5. ✅ Maven 有效 POM 显示正确的版本配置
6. ✅ 未启用预览特性（符合 D-16 决策）
7. ✅ BUILD-01 到 BUILD-04 需求已实现
</success_criteria>

<output>
完成本计划后，创建 `.planning/phases/01-build-environment-code-standards/01-01-SUMMARY.md` 总结文件。
</output>